# 구현 현황 및 구조 평가

> 작성 기준: 2026-04-25 / 실제 코드 기준 (개선5 + 개선6 + 개선7~8 + 메모리 3계층 구조 + 학습 파이프라인 개선 반영)
> 각 모듈의 동작 상태, 맞물림 여부, 개선 가능성을 정리한다.
>
> **테스트 정책**: 모든 테스트 파일은 반드시 `tests/` 폴더에 저장한다.

---

## 전체 로직 흐름 (눈으로 확인 가능한 진입점 기준)

### ─ Conversation 흐름 ─

```
사용자 입력 (UI 채팅창 or CLI)
    │
    ▼
[UI/UX] bridge.py :: sendMessage()
    │   └─ LLMWorker(QThread) 백그라운드 실행
    │
    ▼
[Agent] Agent.handle_input(mode="chat")
    │
    ▼
[Conversation] ConversationRouter.handle_turn()
    │
    ├─ ① [RAG] world_nav.detect_move_intent()
    │        키워드 필터 → LLM(max_tokens=15) 장소 추출
    │        └─ 이동 감지 시: find_or_create_location()
    │               YAML acts 매칭 or LLM 새 장소 생성
    │               → VDB(world_knowledge) upsert
    │               → session.location_context 갱신
    │
    ├─ ① [NarrationMonitor] check_keyword()  ← (bridge.py 경유 선택적)
    │        키워드 감지 → narration_hardcoded.find_trigger()
    │        세션 1회 제한(_fired_keywords) → 묘사 텍스트 채팅 버블 삽입
    │
    ├─ ① [WorldTrigger] check_story_trigger / check_culture_trigger  ← (router._check_world_triggers)
    │        story: 사용자 발화 ↔ trigger_keywords 절대 점수 ≥ 0.9 + 토큰 부분 매칭(0.5 가중치) 이중 판정
    │             → session.fired_stories 중복 방지 → 나레이션 출력
    │        culture: LLM 의도 분류 → explained_cultures 소거법 → 미설명 항목 출력
    │        place: session.visited_places 기반 최초 진입 시 출력
    │        모두 세션 내 최초 1회 발동 (fired_stories / visited_places / explained_cultures 추적)
    │
    ├─ ② [Memory] short_term.get_recent()
    │        session.dialogue_log 슬라이딩 윈도우
    │        → Layer D (단기 히스토리 300토큰)
    │
    ├─ ③ [VDB] LongTermMemory.query()
    │        ChromaDB {char_id}_memory 컬렉션
    │        cosine 유사도 ≥ 0.52 필터
    │        → Layer C (장기 기억 150토큰)
    │
    ├─ ④ [RAG] WorldRetriever.query()
    │        ChromaDB world_knowledge 컬렉션
    │        cosine 유사도 ≥ 0.52 필터, 상위 2개
    │        → Layer B (세계관+act 200토큰)
    │
    ├─ ⑤ [Conversation] PromptBuilder.assemble()
    │        Layer A(캐릭터 시스템 300) + B + C + D(450) + E(session_context + character_notes)
    │        → messages 리스트
    │
    ├─ ⑥ [LLM] LLMClient.generate()
    │        transformers(GPU bfloat16) or llama_cpp(CPU GGUF)
    │        repetition_penalty=1.1 / max_new_tokens=512
    │        → 캐릭터 응답 문자열
    │
    ├─ ⑦ [Conversation] update_mood() / update_affection()
    │        사용자 발화 키워드 → mood 8종 판정
    │        mood별 affection 증감 (캐릭터 YAML delta 우선)
    │        aff_delta 추적 (즉시 flush 조건 판단용)
    │
    ├─ ⑦-b [Memory] short_term.evict_to_context()
    │        dialogue_log > SHORT_TERM_N(5) 시 가장 오래된 턴을 session_context에 누적 (최대 600자)
    │
    ├─ ⑦-c [Conversation] router._PROMISE_RE 감지
    │        어시스턴트 응답에서 "~할게/~기억할게" 패턴 감지 → session.character_notes 추가
    │
    ├─ ⑧ [Memory/VDB] summarizer.check_trigger()
    │        N턴(=5)마다 또는 triggered/aff_delta≥5 즉시 flush: LLM 요약(산문) → 중요도 scoring(≥0.65)
    │        mode=="function" 턴은 수집 제외 (chat 턴만 대상)
    │        → ChromaDB {char_id}_memory 저장 (cosine dedup 0.85, quota 200, TTL 30일)
    │
    └─ ⑨ [학습 로그] ConversationLogger.log_turn()
             사용자 발화 키워드 카테고리 분류
             (daily/emotion/memory/advice/persona/neg/pos)
             Jaccard 중복 제거(0.55) → training/log/{cat}/*.jsonl
    │
    ▼ (응답 반환)
[UI/UX] bridge.py :: _sync_state()
    mood/배경 변화 감지 → backgroundChanged / moodChanged 시그널
    → QML CharacterDisplay / PipWindow / 배경 이미지 갱신
```

---

### ─ Tools 흐름 ─

```
사용자 입력 (대화 중 기능 요청)
    │
    ▼
[Agent] Agent.handle_input(mode="function")
    │
    ▼
[Tools] _select_tool()  ← agent/core.py _KEYWORDS 순서 매칭
    │
    ├─ "프롬프트/prompt/명확하게/간결하게/상세하게/질문형/지시형"
    │       → PromptConverterTool  (tools/prompt_converter.py)
    │
    ├─ "이름/rename/renamer/파일명"
    │       → RenamerTool          (tools/folder/renamer.py)
    │
    ├─ "분류/정리/폴더"
    │       → ClassifierTool       (tools/folder/classifier.py)
    │
    ├─ "이미지/image/png/jpg/jpeg/webp/bmp/tiff"
    │       → ConverterTool        (tools/folder/converter.py)
    │
    └─ "검색/search/찾아/파일 찾"
            → LocalSearchTool      (tools/search/local_search.py)
    │
    ▼
[Agent] _inject_prompt_guide(tool_name)  ← prompt_convert 제외
    │   ChromaDB prompt_guides 컬렉션 조회 (model 키 exact match)
    │   → 시스템 프롬프트에 "=== 가이드 (tool_name) ===" 블록 주입
    │
    ▼
[LLM] LLMClient.generate(tool.system_prompt + 사용자 입력)
    파라미터 JSON 추출 (BaseTool.parse_params)
    │
    ▼
[Tools] tool.execute(params)  ← rule-based, LLM 추론 없음
    │
    ├─ PromptConverterTool  → 스타일 재작성 (5종)
    ├─ RenamerTool          → 파일명 일괄 변경 (7규칙, dry_run)
    ├─ ClassifierTool       → MIME/확장자 기준 폴더 분류
    ├─ ConverterTool        → Pillow 이미지 포맷 변환
    └─ LocalSearchTool      → SQLite FTS5 증분 인덱싱·검색
    │
    ▼
결과 문자열 반환 → 사용자에게 출력
```

---

### ─ 학습 파이프라인 흐름 (별도 실행) ─

```
[학습 로그] training/log/{category}/*.jsonl   ← Conversation ⑨에서 수집
    │
    ▼  (수동 실행)
scripts/build_dataset.py
    glob("**/*.jsonl") → ChatML 변환 → stratified sampling
    → data/lora/conversation/*.jsonl
    │  ✅ glob("**/*.jsonl") 수정 완료 (2026-03-22)
    │
    ▼  (선택) EWC Fisher 계산 (이전 어댑터 기반)
training/ewc.py
    base model + adapter 로드 → LoRA 파라미터 Fisher 대각 계산
    → output/LoRA_vN/fisher.pt, ref_params.pt
    │
    ▼  (수동 실행 — 직접 또는 train_monitor.py 경유)
training/train_monitor.py  [선택]
    학습 명령어를 받아 subprocess로 lora_train.py 실행
    trainer_state.json 폴링 → 과적합 감지 시 SIGTERM
    VRAM 해제 → 학습 상황/종료 조건/입력 명령어 출력
    │
    ▼
training/lora_train.py
    bfloat16 LoRA / assistant 토큰 마스킹 / loss 그래프 PNG
    EWCTrainer: CE loss + λ/2 × Σ F_i × (θ_i - θ*_i)²  (--ewc_lambda > 0 시)
    category_weights 오버/언더샘플링  (--category_weights 지정 시)
    → output/{run_name}/adapter/  저장
    → checkpoint-* 삭제
    │
    ▼  (자동: --skip_eval 없을 때)
VRAM 완전 해제
    trainer.model=None / del model, tokenizer
    gc.collect() / torch.cuda.empty_cache() + synchronize()
    │
    ▼  (subprocess, 각각 독립 프로세스 — train_monitor 또는 lora_train이 실행)
training/eval/ai_tell_checker.py  ← AI투 표현 패턴 측정
training/eval/memory_test.py      ← 기억 유지 정확도 (5케이스)
training/eval/scenario_eval.py    ← 고정 18개 시나리오 PASS/FAIL

    (수동 실행)
training/eval/speed_bench.py      ← 추론 속도 벤치마크
training/eval/verify_phases.py    ← Phase 2/3 실환경 검증 (12턴)
training/eval/klue_regression.py  ← KLUE 회귀 (lm-eval 필요, 선택적)
```

---

## 1. Conversation

### 현재 상태
- `router.py::handle_turn()`이 전체 파이프라인을 순서대로 실행. 완전 동작 중.
- Layer 구조 (PromptBuilder): A(캐릭터 시스템) 300토큰 + B(세계관+act) 200토큰 + C(VDB) 150토큰 + D(단기히스토리) **450토큰** + E(session_context+character_notes) + 생성 512토큰.
- mood 8종 / affection 6단계 / location_context 동적 업데이트 모두 연결됨.
- **세계관 트리거 시스템** (2026-04-09, 개선5 항목3):
  - `narration/world_trigger.py` 신규 — `check_story_trigger()` / `check_place_trigger()` / `check_culture_trigger()`
  - story 트리거: 사용자 발화 ↔ trigger_keywords 절대 점수 ≥ 0.9 + 토큰 부분 매칭(0.5 가중치) 이중 판정 (세션 내 최초 1회)
  - `conversation/core/session.py` — `fired_stories` / `visited_places` / `explained_cultures` 트리거 상태 필드 추가
  - `conversation/core/router.py` — `handle_turn(mode=)` 파라미터 추가. mode="function"이면 요약 트리거 건너뜀. `_check_world_triggers()` 추가
  - `conversation/session_manager.py` — `SessionMeta.world_id` 추가, `activate_for_world(char_id, world_id)` 신규
- **개선7 — 캐릭터 시스템 확장** (2026-04-20):
  - `CH_MookHyeon.yaml` 신규 캐릭터 완성 (존댓말 / affection 6단계 / emotion 8종 / rules 7개)
  - `conversation/character/character_schema.yaml` 스키마 문서화 갱신
  - `CharacterBuildPanel.qml` 신규 — YAML 슬롯 편집 UI
- **개선8 — 대화 품질 개선** (2026-04-20):
  - `agent/core.py` `mood_decay` — 매 턴 mood를 neutral 방향으로 서서히 감쇠 / `mood_hold: int` — 강한 감정 이벤트 발생 시 N턴 동안 decay 정지
  - `memory/summarizer.py` 요약 구조화 — 요약 결과를 "이름:", "감정:", "사건:" 등 필드 분리 저장, 검색 품질 향상
  - `conversation/core/router.py` RAG 지시문 삽입 — world_context 주입 시 모델이 맥락을 참고하도록 명시적 지시문 prefix 추가
  - `model_version` 메타데이터 — 응답 생성 시 adapter 버전 기록 (디버깅 용도)
- **대화개선 6~8절 계획 수립** (2026-04-21):
  - 할루시네이션 7가지 유형 분석 + 코드 수준 원인 특정 (K~P 항목)
  - 3계층 메모리 구조 설계 (`session_context` eviction + summarizer 개선, Q~Z 항목)
  - 컨텍스트 관리 전략 (flush 트리거·VDB TTL·저장 비용, V~Z 항목)
  - → 미구현 상태, `docs/로드맵.md` 1~3단계 참조
- **UI 버그 수정 및 개선** (2026-04-21):
  - `ui_ux/qml/ManualPanel.qml` 신규 — 기능 설명 패널 (? 버튼)
  - `bridge.py` `getHelpByKeyword()` 추가 — 키워드 매칭 점수 기반 도움말 반환
  - 기능 모드 해시태그 자동 대화모드 전환 버그 수정 (5곳 제거)
  - `#?` 도움말 태그 동작 일관성 수정 (사용 후 태그 유지, 모드 유지)
- **메모리 3계층 구조 구현** (2026-04-24):
  - `session.py` — `session_context: str` (evict된 이전 대화 텍스트), `character_notes: list[str]` (세션 내 약속 목록) 필드 추가
  - `memory/short_term.py` — `SHORT_TERM_N=5` / `evict_to_context()` 신규: dialogue_log가 5턴 초과 시 가장 오래된 1턴을 session_context에 누적 (최대 600자)
  - `prompt_build.py` — `_layer_e()` 신규: session_context(`[이전 대화 요약]`) + character_notes(`[이번 세션 약속]`) 통합 주입. Layer D 예산 300 → **450토큰**.
  - `router.py` — `_PROMISE_RE` 정규식으로 assistant 응답에서 약속 패턴("~할게/기억할게 등") 감지 → `character_notes` 자동 추가. `aff_delta` 추적 + triggered 시 summarizer 즉시 flush (background thread).
  - `session_manager.py` — `reset_current()`: 세션 대화·context·트리거 상태 초기화. `delete_session()`: 세션 완전 삭제 + 다음 세션 자동 전환. `SessionMeta.session_context` 스냅샷 추가.
  - `bridge.py` — `resetSession()` 슬롯 신규: session_context / mood / affection / turn_count 초기화.
  - `prompt_build.py` 추가: Layer A rules 포맷 `[규칙]\n- ` 리스트 형식으로 변경. Layer B RAG 지시문 "캐릭터 말투로 재가공" 문구 구체화. `_layer_a()` 조립 결과 `"\n".join()` 전환.
- **CH_Haru.yaml rules 3개 추가** (2026-04-21):
  - 반말 종결어미 명시 강제, 줄임표 최대 1회 제한, 취향 요소 맥락 무관 삽입 금지
- **conversation_logger 리팩터** (2026-04-21):
  - 중요도 기반 동적 청크 크기, Jaccard dedup, `edit_turn()` 추가
  - `monitor.py` / `review.py` 버그 수정 (session_id 파일명 대응, 줄번호 표시 오류)
- **세계관 트리거 품질 개선** (2026-04-10, 대화품질 개선):
  - `_CULTURE_KEYWORDS` 정제: `"어떻게"` / `"특색"` / `"특징"` 제거 (너무 일반적인 단어가 문화 트리거 과발동 유발)
  - 모든 트리거 함수 반환값 정제: 기존 `f"[{item_title}]\n{document}"` → 순수 `document` 텍스트 반환 (나레이션 버블에 DB 덤프 형식 노출 방지)
- narrator.py(LLM 나레이터) 제거 — `narration_hardcoded.py` + `narration_monitor.py`로 대체.
  - `find_trigger()`: 키워드(카페/공원/비/커피 등) → 미리 작성된 묘사 텍스트 반환. LLM 호출 없음.
  - `NarrationMonitor.check_keyword()`: 세션 내 키워드당 1회만 발동 (`_fired_keywords` 추적).
- **`PromptBuilder._layer_a()` 개선** (2026-03-29):
  - YAML `rules` 필드를 직접 join하여 시스템 프롬프트에 반영 (이전: 하드코딩 요약으로 무시됨)
  - 이름 형식 `"너의 이름은 X이다."` → `"너는 X이다."` 로 학습 데이터 포맷과 통일
  - CH_Haru.yaml / CH_Seonjae.yaml 에 추가된 언어/문법/stranger tier 규칙이 Layer A에 포함됨
- **존댓말 지시 강화** (2026-04-10, 대화품질 개선):
  - `speech.formality == "존댓말"` 시 단순 안내문 → `"반드시 존댓말(경어체)로만 말한다. 반말을 절대 사용하지 않는다."` 강력한 금지 문장으로 교체
  - `"반말"` 동일하게 강화: `"반드시 반말로만 말한다. 존댓말을 사용하지 않는다."`
  - CH_MookHyeon.yaml 신규 생성: `speech.formality: 존댓말` + `affection` 6단계(각 존댓말 강조) + `emotion` 8종 + `rules` 7개(AI투/언어/문법/stranger 규칙 포함)
  - `CharacterCreatePanel.qml` `_buildJson()` 개선: formality 분기 기반 `affection` 6단계 자동 생성 + `emotion` 8종 자동 생성 → 캐릭터 생성 즉시 완전한 YAML 스키마 충족
- **캐릭터 스키마 기반 리팩토링** (2026-04-02):
  - `conversation/character/character_schema.yaml` 신규 — 모든 CH_*.yaml의 계약(contract). affection/emotion/speech/personality 슬롯 정의.
  - `CH_Haru.yaml` 전면 재작성: `speech_style` (monolithic) → `speech: {formality/style/persona}` 슬롯 분리. `state.tone_guide` → 최상위 `affection:` 슬롯으로 이동. `emotion:` 슬롯 신규 추가.
  - `prompt_build.py _layer_a()` 스키마 슬롯 기반 조립으로 전환: `_STYLE_PRESETS` / `_PERSONA_PRESETS` / `_PERSONALITY_PRESETS` preset 해석 + YAML 슬롯 우선/폴백 패턴.
  - 조립 순서: name → description → speech.formality → speech.style(preset) → speech.persona(preset) → personality(preset) → affection[tier](YAML 우선) → emotion[mood](neutral 생략) → conv_hints → rules

### 맞물림 이슈
- **mood 트리거가 사용자 입력 기준**: LLM 응답이 아닌 사용자 발화 키워드로 mood를 판단. 캐릭터가 실제로 어떤 감정을 표현했는지는 반영 안 됨.
- ~~**session에 location 필드 없음**~~: ✅ `session.location` 필드 추가, router.py에서 장소 이동 시 자동 갱신, bridge.py YAML 역참조 제거 (2026-03-22).
- **UI와의 연결**: `bridge.py::_sync_state()`가 응답 후 mood/배경 변화를 감지해 시그널 emit — 정상 연결.

### 개선 가능성
- narration_hardcoded 확장: 키워드 목록·묘사 텍스트를 늘리면 몰입감 향상. LLM 호출 없이 즉시 동작.
- ~~session에 `location` 필드 추가~~: ✅ 완료 (2026-03-22).
- mood를 LLM 응답 분석 기반으로 전환하면 정확도 향상 (현재는 단순 키워드 매칭).

### 테스트 구현 항목 (`tests/`)
- `test_router.py`: handle_turn() 파이프라인 순서 검증, mood/affection 상태 갱신 확인
- `test_prompt_builder.py`: 토큰 예산 초과 여부, Layer A~D 조립 결과 형식 검증
- `test_session.py`: from_character() 초기값, add_turn() 로그 누적 확인
- ~~`test_dialogue_quality.TestPromptBuildLayerA`~~: ✅ Layer A 이름 형식, rules 포함, tier 톤 3개 테스트 구현 완료 (2026-03-29)
- ~~`test_narration.TestFindTrigger`~~: ✅ find_trigger() 키워드 매칭, 긴 키워드 우선 매칭 5개 테스트 완료
- ~~`test_narration.TestNarrationMonitorKeyword`~~: ✅ check_keyword() 세션 1회 제한, 인스턴스 리셋 6개 테스트 완료
- ~~`test_narration.TestBridgeActionPattern`~~: ✅ _ACTION_RE *...* 패턴, (행동: ...) 변환 4개 테스트 완료
- ~~`test_improvement5.TestConversationSessionTriggerFields`~~: ✅ fired_stories / visited_places / explained_cultures 초기값 3개 테스트 완료 (2026-04-09)
- ~~`test_improvement5.TestRouterModeParameter`~~: ✅ mode="chat"/"function" 분기 3개 테스트 완료 (2026-04-09)
- ~~`test_improvement5.TestSessionManagerWorldId`~~: ✅ SessionMeta.world_id / activate_for_world() 4개 테스트 완료 (2026-04-09)
- ~~`test_improvement5.TestWorldTriggers`~~: ✅ story/culture/place 트리거 5개 테스트 완료 (2026-04-09)
- ~~`test_improvement5.TestRagIndexParser`~~: ✅ _parse_world_md() 섹션 파서 3개 테스트 완료 (2026-04-09)

---

## 2. Memory / 학습 로그 → VDB 저장

### 현재 상태

**대화 메모리 (런타임) — 3계층 구조**
- **단기(short_term)**: `session.dialogue_log` (최대 SHORT_TERM_N=5턴). 초과 시 `evict_to_context()`로 가장 오래된 턴을 session_context에 누적. 완전 동작.
- **중기(session_context)**: evict된 턴들의 압축 텍스트 (최대 600자). Layer E로 `[이전 대화 요약]` 블록에 주입. 세션 종료 시 소멸 (VDB 저장 안 됨).
- **장기(long_term)**: ChromaDB VDB. 요약 후 저장, 관련 입력 시 검색해 Layer C에 삽입. 완전 동작.
  - **cosine 중복 제거** (2026-04-24): 저장 전 기존 항목과 cosine 유사도 계산, 0.85 이상이면 신규 ID 대신 기존 항목 upsert.
  - **quota 관리** (2026-04-24): 캐릭터당 최대 200개 제한 (`_enforce_quota()`). 초과 시 importance 낮은 항목부터 삭제.
  - **TTL 만료** (2026-04-24): `prune_expired()` — importance < 0.90 + 저장 30일 초과 항목 자동 삭제. importance ≥ 0.90은 영구 보관.
  - 검색 threshold: `importance >= 0.5` → **`>= 0.65`** 상향 (저품질 기억 검색 노출 감소).
- **요약(summarizer)**: N턴마다 또는 즉시 flush 시 LLM 요약 → 중요도 scoring → VDB 저장. 완전 동작.
  - **프롬프트 산문화** (2026-04-24): 구조화 형식("이름:/사건:/감정:/기타:") → 2~4문장 자연어 산문으로 변경. 요약 품질 향상, max_tokens 150 → 250.
  - 중요도 기준: high 키워드(이름/약속/갈등) → 0.85 / mid 키워드(취미/감정/날짜) → 0.6 / 무키워드 → **0.0** (키워드 미매칭 시 VDB 저장 안 됨)
  - 저장 threshold: `score < 0.5` → **`score < 0.65`** 상향.

**학습 로그 수집 (ConversationLogger)**
- 대화 중 유저 발화 키워드를 분석해 카테고리(daily/emotion/memory/advice/persona/negative/positive) 분류.
- 트리거 조건: 키워드 매칭 즉시 flush / 반복 루프(assistant 응답 3회 동일) 즉시 flush / CHUNK_SIZE 턴 도달 / 세션 종료.
- Jaccard 유사도 기반 중복 제거 후 `training/log/{category}/YYYY-MM-DD.jsonl` 저장.
- `scripts/build_dataset.py`가 이 로그를 읽어 ChatML 포맷 학습 데이터로 변환 → LoRA 학습에 사용.

### 맞물림 이슈
- 장기 메모리 컬렉션명이 `{character_id.lower()}_memory` — 캐릭터 변경 시 새 컬렉션으로 자동 전환됨. 정상.
- 요약 트리거 간격(`memory_trigger_n=5`) — aff_delta ≥ 5 또는 world_trigger 발동 시 즉시 flush로 중요 이벤트 누락 방지.
- ~~**단기↔장기 경계 불명확**~~: ✅ session_context(중기) 계층 추가로 단기→중기→장기 3계층 구조 완성 (2026-04-24).
- ~~**`build_dataset.py`의 glob 버그**~~: ✅ `glob("**/*.jsonl")` 수정 완료 (2026-03-22).
- `training/log/advice/`, `persona/` 폴더 미존재 → 해당 카테고리 로그 수집 안 됨. (기능 동작은 하나 log 미수집 — 폴더 생성 시 자동 수집 시작)

### 개선 가능성
- 중요도 threshold(현재 0.5)를 캐릭터별로 조정 가능하게 하면 캐릭터마다 기억 민감도 차별화 가능.
- 요약 시 affection·mood 변화 이벤트를 별도 메타데이터로 저장하면 "우리가 처음 친해졌을 때" 같은 맥락 검색 품질 향상 가능.
- 로그 카테고리에 mood별 감정 반응 데이터 추가(affectionate/touched/curious/embarrassed/angry) 시 감정 표현력 향상.

### 테스트 구현 항목 (`tests/`)
- `test_conversation_logger.py`: 카테고리 분류 키워드 매칭, Jaccard 중복 제거, flush 트리거 조건
- `test_summarizer.py`: N턴 트리거, 중요도 scoring 임계값, VDB 저장 형식
- `test_build_dataset.py`: glob 수정 후 서브폴더 파일 탐색 확인, ChatML 변환 형식 검증

---

## 3. VDB

### 현재 상태
- ChromaDB PersistentClient 사용. `chroma_dev/` 로컬 디스크 저장.
- 세 가지 용도로 분리 (2026-04-09, 개선5 항목6·7):
  - `world_knowledge` 컬렉션 → 세계관 RAG (섹션 기반 청킹)
  - `{char_id}_memory` 컬렉션 → 캐릭터별 장기 메모리
  - `prompt_guides` 컬렉션 → 기능모드 프롬프트 가이드 (메타: `model_name` / `model` / `character_id`)
- 임베딩 모델: BAAI/bge-m3 (384차원, 한국어 최적화). cosine 유사도. threshold 0.52.

### 맞물림 이슈
- **동일 ChromaDB 인스턴스 공유**: 세계관 RAG와 장기 메모리가 같은 DB를 쓰지만 컬렉션이 달라 분리는 됨. 단, DB 파일 하나가 깨지면 전체 영향.
- **배포 환경은 `chroma_deploy/` 별도 경로**: dev↔deploy 데이터 분리는 되어 있음.
- `ui_test` 환경에서는 임베딩 모델 None → VDB 기능 동작 안 함. 테스트 중 장기 메모리/RAG 검색 불가.

### 개선 가능성
- 세계관 RAG용 컬렉션과 메모리 컬렉션을 물리적으로 다른 DB로 분리하면 백업/마이그레이션 용이.
- threshold를 쿼리 유형(세계관 vs 메모리)별로 다르게 설정할 여지 있음 (현재 둘 다 0.52).

### 테스트 구현 항목 (`tests/`)
- `test_vdb.py`: 저장→검색 왕복, threshold 필터 동작, 컬렉션 분리(world_knowledge vs _memory)
- ui_test 환경에서 임베딩 None일 때 조용한 폴백(검색 결과 빈 리스트 반환) 확인

---

## 4. RAG

### 현재 상태
- **`rag/index.py`** (2026-04-09, 개선5 항목2): `sources/world/Seaside.md` 읽기 → 섹션 기반 청킹으로 전환.
  - `_parse_world_md()` 신규: `# WorldName / ## section / ### item_title` 구조 파싱. story 전용 `트리거 키워드: [...]` 파싱.
  - ChromaDB 메타: `{world_id, section, item_title, trigger_keywords}` 삼중 키 기반 식별.
  - 기존 400자 고정 크기 청킹(`_chunk_text()`) 제거.
- **`rag/retrieve.py`**: `WorldRetriever.query()` 기존 동작 유지. `query_by_meta(world_id, section)` 신규 — world_id + section 필터 기반 전체 항목 반환.
- `rag/sources/world/Seaside.md` (2026-04-09, 신규 / 2026-04-10, 소설체 전면 재작성): 기존 culture.md + place.md + story.md 통합. `## culture / ## place / ## story` 섹션 구조. 나레이션 버블 출력을 고려해 건조한 설명문 → 소설 서술체 산문 스타일로 전환. culture 항목에 `트리거 키워드: [...]` 헤더 포함.
- `rag/world_nav.py`: 이동 의도 감지 → YAML acts 매칭 or LLM으로 새 장소 생성 → VDB에 동적 저장.

### 맞물림 이슈
- **세계관 문서가 현재 1개**(seaside_world): 섹션 기반 청킹으로 의미 단위 보존 향상됨.
- **world_nav가 LLM을 2회 호출**: detect_move_intent(LLM) + find_or_create_location(LLM). 매 턴 실행 시 지연 가능성.
- 동적 생성된 장소가 VDB에 저장되지만 YAML에는 반영 안 됨 → 세션 재시작 시 YAML에 없는 장소가 VDB에만 남음.

### 개선 가능성
- ~~고정 크기 → `##` 헤더 기준 semantic chunking으로 전환~~: ✅ 섹션 기반 청킹으로 전환 완료 (2026-04-09).
- 이동 의도 감지를 LLM 호출 없이 키워드 사전 기반으로만 처리하는 fast-path를 두면 레이턴시 감소.
- 동적 생성 장소를 YAML에도 병합 저장하는 로직 추가 시 세션 간 일관성 유지 가능.

### 테스트 구현 항목 (`tests/`)
- `test_rag_index.py`: 청킹 결과 청크 수·길이 확인, ChromaDB 저장 확인
- `test_rag_retrieve.py`: threshold 이상/이하 문서 필터링, 상위 N개 반환 확인
- `test_world_nav.py`: 이동 의도 감지 키워드 매칭, YAML acts 매칭 로직
- ~~`test_improvement5.TestRagIndexParser`~~: ✅ _parse_world_md() 섹션 파서 3개 테스트 완료 (2026-04-09)

---

## 5. Training

### 현재 상태
- `lora_train.py`: LoRA (bfloat16, assistant 토큰 마스킹, loss 그래프 자동 저장, `EWCTrainer`). 완성됨.
  - **BUG-14 수정** (2026-03-28): 체크포인트 삭제 전 `trainer.save_state()` 호출 추가 → `trainer_state.json` 보존, `train_monitor.py` 최종 보고 정상화.
  - 어댑터 저장 + checkpoint-* 삭제 완료 후, **VRAM 완전 해제** (학습 중 ~96% 점유):
    `trainer.model=None` → `del trainer/model/tokenizer` → `gc.collect()` → `torch.cuda.empty_cache() + synchronize()`
  - VRAM 해제 확인 후 `training/eval/` 스크립트를 **subprocess**(독립 프로세스)로 순차 실행.
  - `--skip_eval`로 평가 전체 건너뜀 가능. `--no_save`이면 평가 실행 안 함.
  - `--ewc_fisher` / `--ewc_ref_params` / `--ewc_lambda`: EWC 다단계 연속 학습 지원.
  - `--category_weights`: 카테고리별 오버/언더샘플링 JSON 인자.
- `ewc.py`: Fisher 대각 계산 CLI + `EWCPenalty` 클래스. LoRA 파라미터만 대상으로 Fisher 추정 후 `fisher.pt` / `ref_params.pt` 저장. 완성됨.
- `dataset.py`: ChatML 포맷, stratified sampling, `category_weights` 오버/언더샘플링, max_length 필터. 완성됨.
- 학습 데이터: `training/data/` 하위 총 **66파일, 3,632건** (v12 기준).
  - v9→v10 변경: personality bright/dependent 제거, speech_style formal/mixed 3개 제거 (`_excluded/`로 이동). long_dialogue 4개 신규 추가. 합계 2,401→2,150건.
  - v10→v11 변경 (2026-04-14): +1,020건, +19파일. affection/formal 신규, emotion 8종 확장, long_dialogue 확장.
  - **v11→v12 변경 (2026-04-24): +462건, +9파일.** 메모리·감정·세계관 3개 영역 신규 추가.
    - `memory/vdb_context_use` (40건): VDB 기억 자연스러운 활용 패턴.
    - `memory/session_context_ref` (30건): 이전 대화 요약 참조 패턴.
    - `memory/promise_followup` (20건): 약속 이행 패턴.
    - `memory/summarizer_sft` (50건): 산문 요약 생성 능력.
    - `emotion/action_desc` (39건): `*...*` 행동 묘사 감정 표현.
    - `emotion/suppressed` (30건): 감정 억제·내면화 패턴.
    - `emotion/affection_cross` (40건): 친밀도×감정 교차 패턴.
    - `long_dialogue/emotion_arc` (20건): 감정 호 변화 멀티턴.
    - `common/world_context_use` (40건): 세계관 RAG 자연 활용 패턴.
    - `common/world_trigger_response` 기존 파일 +30건 확장.
  - 시스템 프롬프트 형식 전환 (2026-04-02): `너는 하루이다.` 고정 → `너는 {char_name}이다.` 플레이스홀더.
- 현재 어댑터: v12 학습 진행 중 (step 100/1503 재시작 — eval best 1.862 at step 100, epoch 0.2). v11 어댑터 `output/LoRA_v11/adapter/` (eval best 1.5387 at step 700).
  - v9 대비: 데이터 2,150건, {char_name} 플레이스홀더 + character_schema 정합 적용.
  - v10_korean(MyeongHo0621/Qwen2.5-3B-Korean 기반) 비교 실험 — 입력 재조립 패턴 관측, 채택 보류.
- **학습 데이터 파이프라인 추가** (2026-03-28/29):
  - `training/scripts/build_sft_from_feedback.py` (신규): `training/log/feedback_pos/` → ChatML SFT 변환. affection low/mid/high → stranger/familiar/close tier 매핑. `--reviewed_only`, `--dry_run` 지원.
- **`build_sft_from_feedback.py` `_build_system_prompt()` 수정** (2026-04-10, 대화품질 개선):
  - 구: `char.get("speech_style")` / `char["state"]["tone_guide"]` 참조 → 현재 YAML 스키마에 없는 필드로 항상 빈 시스템 프롬프트 생성됨.
  - 신: `from conversation.core.prompt_build import _STYLE_PRESETS, _PERSONA_PRESETS, _PERSONALITY_PRESETS, _AFFECTION_FALLBACK` 임포트 + `_AFF_TIER_MAP = {"low": "stranger", "mid": "familiar", "high": "close"}` 추가. `PromptBuilder._layer_a()`와 동일한 조립 순서로 시스템 프롬프트 생성 → 학습-추론 일관성 확보.
- **학습 로그/데이터 품질 정리** (2026-04-10):
  - `training/log/daily/c9b01ad6.jsonl` 삭제: 빈 user 발화 + HTML 검색결과가 assistant 응답으로 기록된 기능모드 오로그.
  - `training/data/common/ai_tell_removal.jsonl` L14/L21 주석처리: 존댓말+AI보조 어투(`"목표가 뭔지 먼저 말씀해 주세요."`), AI 투 표현 혼입(`"좋은 질문이에요? 아니, 그냥 그런 날도 있지."`) — `//` 비활성화.
  - `training/log/_schema.json` 업데이트: `affection` 3단계→YAML 6단계 매핑 기술 추가, `note` / `reviewed` 필드 설명 추가, `**...**` 나레이션 포맷·기능모드 응답 제외 정책 notes에 추가.
  - `data/lora/conversation/haru_stranger.jsonl` (신규, 15개): affection 0~30 stranger tier 대화 예시. 다음 v10 학습 데이터로 사용 예정.
  - `data/lora/conversation/feedback_sft.jsonl`: `build_sft_from_feedback.py` 출력 대상 (feedback_pos 누적 시 자동 생성).
- **캐릭터 스키마 + 학습 데이터 정렬** (2026-04-02):
  - `conversation/character/character_schema.yaml` 신규: 슬롯 어휘 = training/data 카테고리 구조와 1:1 대응.
  - `training/scripts/rewrite_system_prompts.py` 갱신: CATEGORY_ATTR 텍스트를 CH_Haru.yaml 슬롯 값과 정확히 동기화. 학습-추론 시스템 프롬프트 형식 불일치 해소.
- **`training/eval/`**: 학습 결과 자동/수동 검증 스크립트.
  - 자동 실행 (학습 후): `ai_tell_checker.py` (AI투 표현 측정), `memory_test.py` (기억 유지 정확도), `scenario_eval.py` (고정 18개 시나리오)
  - 수동 실행: `speed_bench.py` (추론 속도), `verify_phases.py` (Phase 2/3 실환경 검증), `klue_regression.py` (KLUE 회귀 — lm-eval 필요)
  - **`scenario_eval.py` 신규** (2026-04-24): 18개 고정 시나리오(메모리 6 / 감정 6 / 세계관 6). rule-based 채점(contains/not_contains/max_len 등). `--judge`로 LLM-as-judge 활성화. 합격 기준 15/18.
  - **`klue_regression.py` 신규** (2026-04-24): lm-eval 래퍼 — klue_ynat + klue_sts 측정. `--save_baseline`으로 베이스 기준점 저장. `--baseline`으로 어댑터 적용 후 비교, 5% 이상 하락 시 경고.
- **`training/lora_train.py` SIGTERM 수정** (2026-04-24):
  - `_sigterm_received` 플래그 + 모듈 수준 `signal.signal(SIGTERM, ...)` 핸들러 → `SystemExit(0)` raise.
  - `trainer.train()` 래핑: `SystemExit` / `KeyboardInterrupt` catch → `early_stopped=True` 분기.
  - 어댑터 저장을 정상 완료·조기 종료 모두 동일 경로(adapter_path)로 통합.
  - eval_scripts에 `scenario_eval.py` 추가 (단독 실행 시 자동 평가 3종 → ai_tell + memory + scenario).
- **`training/train_monitor.py` 개선** (2026-04-24):
  - `TERMINATE_TIMEOUT` 60 → 120초. `--skip_eval` 플래그 추가.
  - 학습 프로세스 시작 시 `--skip_eval` 자동 주입 (lora_train 내부 eval 중복 방지).
  - 프로세스 종료 후 eval 실행 블록 추가: 조기 종료·정상 완료 모두 adapter_path 존재 시 ai_tell → memory → scenario 순 자동 평가.

### 맞물림 이슈
- ~~**`config.py`가 `./output/LoRA_v7/adapter`를 참조**하지만 실제 경로는 `lora_v1/checkpoint-1/`~~: ✅ `output/LoRA_v7/adapter` 실존 확인, 경로 일치 (2026-03-22).
- ~~**`lora_train.py` 학습 후 `trainer_state.json` 손실**~~: ✅ `trainer.save_state()` 추가로 해결 (2026-03-28, BUG-14).
- ~~**`build_dataset.py`의 glob 버그**~~: ✅ `glob("**/*.jsonl")` 수정 완료 (2026-03-22).
- `training/log/advice/`, `persona/` 폴더 미존재 → 해당 카테고리 로그 수집 안 됨. (기능 동작은 하나 log 미수집 — 폴더 생성 시 자동 수집 시작)
- ~~**`config.py`는 LoRA_v7을 참조**~~ → ✅ `output/LoRA_v11/adapter`로 업데이트 완료 (2026-04-14).
- ~~**`lora_train.py` `--data_dir` 기본값 `data/lora`**~~ → ✅ `training/data`로 수정 완료 (2026-03-23).

### 개선 가능성
- ~~학습 데이터에 mood별 감정 반응 데이터가 없음~~: ✅ `emotion/` (9종 × 20건) + `long_dialogue/` (54건) 추가 완료 (2026-03-23).
- ~~EWC 다단계 학습 + 카테고리 가중치 구현 완료 → LoRA_v8 학습 시 적용 예정~~: ✅ v8 학습 완료 (category_weights 적용, EWC 미적용). v9에서 EWC λ=500 적용 예정.
- ~~훈련 데이터 system prompt에 캐릭터 이름("너는 하루다.") 하드코딩~~: ✅ emotion/long_dialogue 234건 정제 완료 (2026-03-23). v9 학습부터 적용.

### 테스트 구현 항목 (`tests/`)
- `test_dataset.py`: ChatML 변환 형식, stratified sampling 비율, max_length 필터 경계값
- `test_lora_train.py`: assistant 토큰 마스킹 적용 여부 (label -100 확인)
- ~~`test_dialogue_quality.TestBuildSftFromFeedback`~~: ✅ SFT 변환, reviewed_only 필터, save() 8개 테스트 구현 완료 (2026-03-29)
- ~~`test_dialogue_quality.TestHaruStrangerJsonl`~~: ✅ JSONL 구조, stranger 톤, 응답 길이 5개 테스트 구현 완료 (2026-03-29)

---

## 6. UI/UX

### 현재 상태
- PySide6 + QML 구조. bridge.py가 Python↔QML 모든 통신 담당.
- 풀 창(432×624) ↔ PIP(160×160) 전환. 배경 이미지, 캐릭터 표시, 설정 패널, 커스터마이징 패널 전부 구현됨.
- **타이틀바**: 캐릭터 이름 + "캐릭터 변경" 버튼(CharacterSelectPanel 오픈) + "상태" 버튼(CharacterStatusPanel 오픈) + ≡ 설정 + PIP 전환 + ✕ 종료.
- **테마 시스템**: ocean(기본) / solar / forest 3종. bridge.getTheme() / saveTheme()으로 `preferences.json`에 영속 저장.
- **CharacterSelectPanel**: 타이틀바 "캐릭터 변경" 버튼 → 모달 패널.
- **CharacterStatusPanel**: 타이틀바 "상태" 버튼 → 이름/tier 배지/친밀도 바/감정/대화 횟수 표시.
- **ResetConfirmPanel**: 설정 패널 "캐릭터 초기화" 버튼 → 확인 모달.
- **기능 모드 UI 오버레이 패널 3종** (2026-03-29 신규 / 2026-03-30 UX 개선):
  - `FileOptionsPanel.qml` — `file_convert` 태그 클릭 시 즉시 파일 선택 다이얼로그 → 이름변경/확장자변경 옵션 패널. IME preedit 씹힘 수정: 적용 버튼에서 `renameInput.text` 직접 읽기 (참조: [BUG_02.md](../BUG/BUG_02.md) 예비 수정).
  - `FolderClassifyPanel.qml` — `folder_classify` 태그 클릭 시 즉시 폴더 선택 다이얼로그 → 종류별/확장자별 분류 기준 선택, 미리보기(dry_run=true) / 분류 시작
  - `FileSearchPanel.qml` — `local_search` 태그 클릭 시 즉시 폴더 선택 → `searchFiles()` → 결과 목록. 이후 검색어 입력 시 저장된 `searchDirectory` 재사용 (재선택 불필요).
- **태그 즉시 다이얼로그 UX** (2026-03-30): 이전에는 메시지 전송 시 다이얼로그가 열렸으나, 태그 클릭 순간 즉시 파일/폴더 선택 창이 열리도록 변경. 취소 시 태그 자동 해제.
- **`#?` 도움말 태그** (2026-03-30): 기능 태그 목록에 `#?` 추가. 클릭 시 전체 기능 설명을 채팅 버블로 즉시 표시.
- **Affection 관리자 슬롯** (2026-03-29): `setAffection()` / `lockAffection()` / `unlockAffection()` + `affectionChanged` Signal + `currentAffection` Property
- **bridge.py 플랫폼 분기** (2026-03-30 신규):
  - `_is_wsl()` / `_is_windows()` 헬퍼 함수로 Windows native / WSL2 / Linux 환경 감지.
  - `openFile()`: WSL2에서 `wslpath` 변환 후 `powershell.exe Invoke-Item` 사용 (cmd.exe /c start는 UNC 경로 미지원으로 PNG 열기 실패 → 수정).
  - `openUrl()` 슬롯 신규 추가: WSL2에서 `cmd.exe /c start ""` 방식으로 HTTP URL 열기. `ChatBubble.qml`의 `onLinkActivated`에서 `Qt.openUrlExternally` 대신 사용.
  - `getHelpText(key)` / `getShownTagIntro()` / `setShownTagIntro(bool)` 슬롯 추가 — `preferences.json` 영속 저장.
- **`_unload_llm()` OOM 수정** (2026-03-30): 캐릭터 전환·새 세션·초기화 시 신규 LLM 로드 전 기존 모델 명시 해제. llama_cpp: `model.close()`, transformers: `del _model + cuda.empty_cache()`. 참조: [BUG_02.md](../BUG/BUG_02.md).
- **Function→Chat 연속성** (2026-03-30): `agent._recent_ops`에 최근 기능 작업 최대 5건 기록. `chat()` 시 Layer F로 시스템 프롬프트에 주입 → 캐릭터가 방금 한 작업을 인지하고 대화 가능.
- **웹 검색 제거** (2026-04-06): `tools/search/web_search.py` 삭제. RAM/VRAM 절감 및 외부 의존성 제거. `agent/core.py` 키워드·슬롯 제거, `bridge.py` helpText 제거.
- **개선5 UI 변경사항** (2026-04-09):
  - `SettingsPanel.qml` — 캐릭터 섹션 "캐릭터 생성" 버튼 추가 (`characterCreateRequested` 시그널). 세션 목록 `display_name` / `{char_name}-{world_id}` 표시. `session_id` null 가드 추가.
  - `MemoryDBPanel.qml` 탭2 — 읽기 전용 → 완전한 CRUD UI. `model_name` 기준 그룹 접힘/펼침. 추가 폼(모델명/캐릭터ID/내용) + 저장/수정/삭제 버튼.
  - `main.qml` — `Component.onCompleted` 기본 세계관 `seaside_world` 자동 적용. `onCharacterCreateRequested` 핸들러 추가.
  - `bridge.py` 신규 슬롯: `getWorldKnowledgeDB` / `addWorldKnowledge` / `updateWorldKnowledge` / `deleteWorldKnowledge` / `reindexWorldKnowledge` / `addPromptGuide` / `updatePromptGuide` / `deletePromptGuide` / `getPromptModelList` / `getDefaultWorld` / `changeWorld` 세계관 변경 감지. `listSessions` — `world_id` 포함. `addPromptGuide` — `model_name` + `model`(정규화 키) 양쪽 저장.
- **개선6~8 UI 변경사항** (2026-04-20):
  - **PIP 말풍선 방향** (`PipWindow.qml`): `bubbleDirection` 프로퍼티(left/right) 추가 — 사용자가 SettingsPanel에서 말풍선 방향을 선택·저장 가능. `bridge.getPipBubbleDir()` / `savePipBubbleDir()` 슬롯 연결.
  - **PIP 드래그 크기 조절**: `resizeRequested` Signal 추가, 드래그로 PIP 창 크기 변경 가능.
  - **EmotionPanel.qml** 신규 — mood/affection 시각화 패널.
  - **CharacterBuildPanel.qml** 신규 — 캐릭터 YAML 슬롯 편집 UI.
- **개선6 UI 변경사항** (2026-04-10):
  - **슬라이더 전면 교체**: `AdminPanel` 친밀도·직접성, `CharacterCreatePanel` 직접성 슬라이더를 `MouseArea { preventStealing: true }` 커스텀 구현으로 교체 — ScrollView Flickable 이벤트 탈취 문제 완전 해결.
  - **응답길이·감정개방도 토글 오류 수정**: 중첩 Repeater에서 `parent.parent.parent.parent` 체인 오류 → 외부 delegate에 `id` + `property int tierIndex: index` 명시 후 직접 참조로 변경.
  - **CharacterStatusPanel 오버플로 수정**: 모달 `height` 공식에 헤더 높이(38px) 누락 → `modalHeader.height + 16 + contentLayout.implicitHeight + 16` + `clip: true`.
  - **MemoryDBPanel CRUD 개선**: 편집 TextEdit를 `Flickable` 래퍼로 교체(100px, 수직 스크롤 가능). 세계관 폼 필드 Text 오버레이 placeholder 추가.
  - **세계관 RAG 트리 구조**: `_worldGroups` 계산을 `world_id → sections[]` 2단계 계층으로 변경. 기존 플랫 표기 대신 `seaside_world > culture/place/story` 중첩 트리.
  - **WorldCreatePanel 신규** (`ui_ux/qml/WorldCreatePanel.qml`): culture/place/story 3섹션, 섹션당 최대 4항목 입력. `SettingsPanel` 세계관 섹션에 `+ 세계관 생성` 버튼 추가 (`worldCreateRequested` 시그널).
  - **`**...**` 나레이션 분리 처리** (2026-04-10, `bridge.py`): `_NARRATION_RE = re.compile(r"\*\*(.+?)\*\*", re.DOTALL)` + `_split_narration(text, default_role) → list[tuple[str,str]]` 함수 신규. 사용자 입력 / 캐릭터 응답 모두에서 `**내용**` 구간을 자동 감지해 `narrator` 버블로 분리 출력. 사용자 입력의 `**...**`는 LLM 전달 시 `(나레이션: ...)` 형태로 변환.
  - **`CharacterCreatePanel.qml` `_buildJson()` 개선** (2026-04-10): formality 분기(`존댓말` / `반말` / 기타) 기반 `affection` 6단계 템플릿 자동 생성 + `emotion` 8종 템플릿 자동 생성. 캐릭터 생성 즉시 `character_schema.yaml` 모든 슬롯을 충족하는 완전한 YAML 구조 출력. 페르소나·성격 UI 라벨에 부가 설명 subtitle 추가.

### 맞물림 이슈
- **emotion/ 폴더 비어있음**: 감정 오버레이 PNG가 없어 CharacterDisplay/PipWindow에서 이모지 플레이스홀더만 표시.
- **characters/ 파츠 폴더 비어있음**: 5종 폴더 구조는 있으나 PNG 없음.
- LLMWorker 실행 중 bridge.py 슬롯이 동시 호출되면 차단 처리가 `_worker is not None` 조건 하나뿐.
- ~~**캐릭터 전환 OOM**~~: ✅ `_unload_llm()` 수정 완료 (2026-03-30). 참조: [BUG_02.md](../BUG/BUG_02.md).
- ~~**WSL2 PNG 파일 열기 실패**~~: ✅ `openFile()` powershell.exe Invoke-Item 전환 완료 (2026-03-30).
- ~~**웹검색 URL 클릭 실패 (WSL2)**~~: ✅ 웹 검색 기능 자체가 제거됨 (2026-04-06).
- ~~**FileOptionsPanel IME 마지막 글자 씹힘**~~: ✅ `renameInput.text` 직접 참조로 수정 완료 (2026-03-30).

### 개선 가능성
- 에셋(emotion PNG, 파츠 PNG)만 배치하면 나머지 UI 연결은 모두 완성된 상태.
- **SideMenuPanel 신규** (2026-04-06): ≡ 버튼 → 오른쪽 슬라이드인 220px 아코디언 패널. DB/설정/관리 섹션.
- **MemoryDBPanel CRUD 신규** (2026-04-06): 카드 목록 + 인라인 수정 폼 + 추가 폼. `memoryChanged` Signal로 실시간 갱신.
- **AdminPanel / CharacterCreatePanel 신규** (2026-04-06): SideMenuPanel 관리 섹션에서 열림.

### 테스트 구현 항목 (`tests/`)
- `tests/test_bridge_slots.py` ✅
  - `TestApplyFileOptions` (7개) ✅ 파일이름 변경, 확장자 변경, 다중 파일 prefix
  - `TestApplyFolderClassify` (5개) ✅ dry_run 검증, 비존재 경로 오류 반환
  - `TestSearchFiles` (5개) + `test_open_file_no_crash` ✅ FTS5 검색, 빈 쿼리/비존재 경로 오류
  - `TestAffectionAdminControl` (6개) + `TestAffectionSemanticGating` (5개) ✅ lock/unlock/gating
  - `TestOpenFileWsl2` (2개) ✅ WSL2 powershell.exe Invoke-Item / cmd.exe 미호출 검증
  - `TestOpenUrl` (2개) ✅ HTTP URL WSL2 cmd.exe /c start 검증
  - `TestHelpAndTagIntro` (5개) ✅ getHelpText(web_search 제거됨) / shown_tag_intro 저장·읽기
  - `TestUnloadLlm` (4개) ✅ llama_cpp close() / transformers del / unload 선행 순서 보장
- `tests/test_ui_structure.py` ✅ QML 속성, 컴포넌트 구조
  - SideMenuPanel / AdminPanel / CharacterCreatePanel / MemoryDBPanel 시그널·속성 검증 포함
- `tests/test_narration.py` ✅ find_trigger / check_keyword / _ACTION_RE 검증
- `tests/test_improvement5.py` ✅ 개선5 전체 커버 (35개 테스트, 2026-04-09)
  - TestConversationSessionTriggerFields (3개) / TestRouterModeParameter (3개) / TestSessionManagerWorldId (4개)
  - TestAgentInjectPromptGuide (4개) / TestBridgePromptGuideKeys (3개) / TestWorldTriggers (5개)
  - TestRagIndexParser (3개) / TestConfigValues (3개) / TestMemoryDBPanelQmlTab2 (7개)
- `tests/test_integration_improvement5.py` ✅ 통합 테스트 11개 (2026-04-10)
  - narrator 버블 emit, world_id 정합성, 트리거 상태 라운드트립, 초기 장소 나레이션
- **전체 테스트: 474개 통과** (2026-04-20, 이전 421개)
- 추가 필요: `test_bridge_concurrency.py` — LLMWorker 실행 중 중복 슬롯 호출 차단 검증

---

## 7. Tools

### 현재 상태
대화 이외의 보조 기능을 LLM 도구 호출 형식으로 제공. `agent/core.py`의 `handle_input(mode="function")`을 통해 진입하며, 슬래시 명령어(`mode="chat"`)와 별도 계층.

**트리거 방식**: 사용자 발화에서 자연어 키워드를 감지(`agent/core.py::_KEYWORDS`) → 도구 선택 → LLM이 파라미터 JSON 추출 → rule-based 실행.

> **기능개선.md 4~7 구현(2026-03-29) + 개선2.md 구현(2026-03-30)으로 변경된 흐름**:
> `file_convert` / `folder_classify` / `local_search` 3개 도구는 태그 클릭 즉시 다이얼로그 → bridge 슬롯 → 도구 실행으로 처리됨.
> `web_search`는 삭제됨 (2026-04-06).
>
> **개선5 항목7 (2026-04-09)**: `prompt_convert` 제외 모든 도구 진입 시 `_inject_prompt_guide(tool_name)` 호출 → ChromaDB `prompt_guides`에서 해당 모델 가이드 조회 → 시스템 프롬프트 앞단에 주입.

| 도구 | 파일 | 태그 키 | 기능 | 실행 경로 |
|---|---|---|---|---|
| `folder_classify` | `tools/folder/classifier.py` | `folder_classify` | 확장자/MIME 기준 폴더 분류 | 태그 클릭→FolderClassifyPanel→`bridge.applyFolderClassify()` |
| `file_convert` | `tools/folder/converter.py` + `bridge.applyFileOptions()` | `file_convert` | 파일명 변경 + 이미지 포맷 변환 (Pillow) | 태그 클릭→FileOptionsPanel→`bridge.applyFileOptions()` |
| `prompt_convert` | `tools/prompt_converter.py` | `prompt_convert` | 프롬프트 스타일 변환 (5종) + ChromaDB 가이드 캐싱 | LLM 파라미터 추출 → `PromptGuideStore` → 크롤링 폴백 |
| `local_search` | `tools/search/local_search.py` | `local_search` | SQLite FTS5 로컬 파일 전문 검색 (서브폴더 포함, os.walk) | 태그 클릭→폴더 선택 저장→검색어 입력→`bridge.searchFiles()` |

> 키워드 우선순위: `prompt_convert` → `file_convert` → `folder_classify` → `local_search` 순.
> web_search 제거됨 (2026-04-06) — RAM/VRAM 절감 및 외부 의존성 제거.

### 맞물림 이슈
- ~~**`commands.py` 비어있음**~~: ✅ 완료 (2026-03-22).
- ~~**`duckduckgo-search` 패키지 이름 변경**~~: ✅ `ddgs>=9.12.0`으로 교체 완료 (2026-03-28).
- ~~**PromptConverter LLM 파라미터 추출 실패 (BUG-F01~F03)**~~: ✅ 수정 완료 (2026-03-28). 참조: [BUG_function.md](../BUG/BUG_function.md).
- ~~**BUG-19: `_PROMPT_CONVERT_MODEL_RE` regex `\w`가 한국어 포함으로 모델명 오추출**~~: ✅ ASCII-only 패턴 + 한국어 비율 상대 임계값으로 수정 완료 (2026-03-29). 참조: [BUG_function.md](../BUG/BUG_function.md).
- ~~**WebSearch 관련 이슈**~~: ✅ web_search.py 전체 삭제 완료 (2026-04-06).
- ~~**LocalSearch 서브폴더 탐색 실패**~~: ✅ `rglob` → `os.walk(onerror=lambda _: None)`로 전환 완료. 권한 오류 시 해당 디렉토리만 건너뜀 (2026-03-30).
- **PromptConverter 변환 대상**: 명확하게/간결하게/상세하게/질문형/지시형 5종. ChatML 포맷 변환 미지원.
- **LocalSearch 캐시 경로**: `~/.cache/achat/` 고정 — 배포 환경에서 경로 충돌 가능성.
- ~~**기능모드 프롬프트 가이드 미주입**~~: ✅ `agent/core.py::_inject_prompt_guide()` 추가 완료 (2026-04-09, 개선5 항목7). `prompt_convert` 제외 모든 도구에 ChromaDB `prompt_guides` 조회 후 시스템 프롬프트에 주입. `tools/prompt_store.py` `model` 키 정규화 호환성 추가.
- ~~**웹검색 결과 저장 미구현**~~: ✅ web_search 기능 자체가 제거됨 (2026-04-06).

### 개선 가능성
- LocalSearch 인덱스 경로를 config.py 환경 분기로 관리하면 dev/deploy 분리 가능.
- `prompt_guides` ChromaDB 컬렉션 — 충분한 데이터 누적 후 시맨틱 검색 정확도 향상 기대.

### 테스트 구현 항목 (`tests/`)
- `test_function_tools.py` ✅
  - ~~`TestWebSearchTwoStep` / `TestWebSearchHyperlinkAndScoring`~~ → 삭제됨 (2026-04-06, web_search.py 제거)
  - `TestPromptGuideStore` (7개) ✅ ChromaDB save/query/delete
  - `TestPromptConvertFallbackUtils` (10개) ✅ regex fallback, 한국어 비율 판정
- `test_integration_flows.py` ✅
  - `TestLocalSearchFullFlow` (10개) ✅ bridge.searchFiles() 서브폴더 포함 전체 흐름
  - `TestFolderClassifyFullFlow` (7개) ✅ bridge.applyFolderClassify() 전체 흐름
  - `TestFileOptionsFullFlow` (8개) ✅ bridge.applyFileOptions() 전체 흐름
  - ~~`TestWebSearchFullFlow`~~ → 삭제됨 (2026-04-06, web_search.py 제거)
- `test_improvement5.py` ✅ TestAgentInjectPromptGuide (4개) / TestBridgePromptGuideKeys (3개) / TestConfigValues (3개) — 프롬프트 가이드 주입 및 키 정규화 검증 (2026-04-09)
- 추가 필요: `test_tools_folder.py` / `test_tool_keyword_routing.py`

---

## 전체 연결 상태 요약

| 연결 | 상태 | 비고 |
|---|---|---|
| Conversation → Memory | ✅ | 단기/장기 모두 매 턴 연결 |
| Conversation → RAG | ✅ | 매 턴 WorldRetriever 실행 |
| Conversation → VDB | ✅ | 요약 트리거로 장기 메모리 저장 |
| Conversation → UI | ✅ | bridge.py 시그널/슬롯 완전 연결 |
| Conversation → 학습 로그 | ✅ | ConversationLogger 매 턴 연결 |
| 학습 로그 → 빌드 → Training | ✅ | glob("**/*.jsonl") 수정 완료 (2026-03-22) |
| Training → Conversation | 🔄 | v12 학습 중 (step 100/1503). 현재 config.py는 LoRA_v11 참조 — v12 완료 후 업데이트 필요 |
| 학습 데이터 ↔ 추론 프롬프트 형식 | ✅ | character_schema.yaml + rewrite_system_prompts.py 동기화 완료 (2026-04-02) |
| session_context → PromptBuilder Layer E | ✅ | short_term.evict_to_context() → session.session_context → _layer_e() 연결 완료 (2026-04-24) |
| character_notes → PromptBuilder Layer E | ✅ | router._PROMISE_RE → session.character_notes → _layer_e() 연결 완료 (2026-04-24) |
| long_term dedup/quota/TTL | ✅ | cosine 중복제거 + quota 200 + TTL 30일 구현 완료 (2026-04-24) |
| 배경 이미지 → UI | ✅ | seaside_world/beach.png 이동 완료 (2026-03-22) |
| Tools → Conversation | ✅ | bridge.sendMessage(mode) → LLMWorker → handle_input(mode) 연결 완료 (2026-03-22). PromptConverter BUG-F01~F05 수정 완료 (2026-03-28) |
| 감정 오버레이 → UI | 🔲 | 구조 준비됨, 에셋 배치 대기 |
| 파츠 커스터마이징 → UI | 🔲 | 구조 준비됨, 에셋 배치 대기 |
| narrator(LLM) → Conversation | ❌ | 제거됨 — narration_hardcoded + NarrationMonitor로 대체 |
| narration_hardcoded → bridge | ✅ | NarrationMonitor.check_keyword() 연결 완료 |
| MemoryDB CRUD → bridge → QML | ✅ | memoryChanged Signal + Connections 연결 완료 |
| SideMenuPanel → main.qml | ✅ | sideMenuOpen 프로퍼티 + Signal 연결 완료 |
| WorldTrigger → ConversationRouter | ✅ | router._check_world_triggers() 연결 완료 (2026-04-09) |
| prompt_guides → agent.core | ✅ | _inject_prompt_guide() 기능모드 진입 시 자동 주입 (2026-04-09) |
| SessionMeta.world_id → SessionManager | ✅ | activate_for_world() + (char_id, world_id) 쌍 기준 세션 관리 (2026-04-09) |
| PromptGuide CRUD → bridge → QML | ✅ | MemoryDBPanel 탭2 CRUD + addPromptGuide model 키 정규화 (2026-04-09) |

---

## 즉시 수정 필요 항목

1. ~~**`config.py` adapter_path**~~ → ✅ `output/LoRA_v11/adapter`로 업데이트 완료 (2026-04-14).
2. ~~**`scripts/build_dataset.py:194`**~~ → ✅ `glob("**/*.jsonl")` 수정 완료 (2026-03-22)
3. ~~**`ui_ux/assets/background/Robby.png`**~~ → ✅ `background/seaside_world/beach.png` 이동 완료 (2026-03-22)
4. ~~**`conversation/loader/chracter_load.py`**~~ → ✅ 해당 없음 — 이 작업공간에 오타 파일 없음
5. ~~**`training/lora_train.py` `--data_dir` 기본값 `data/lora`**~~ → ✅ `training/data`로 수정 완료 (2026-03-23)
6. ~~**기능개선.md 1~7번 구현**~~ → ✅ 전체 완료, 240개 테스트 통과 (2026-03-29)
7. ~~**개선2.md 1/2/3-A/4-A/4-B/5 구현**~~ → ✅ IME 수정·WSL2 파일 열기·URL 열기·태그 즉시 다이얼로그·#? 태그·OOM 해제 완료, 310개 테스트 통과 (2026-03-30)
8. ~~**개선2.md 3-B (웹검색 결과 ChromaDB 저장)**~~ → ✅ web_search 자체 제거로 불필요 (2026-04-06)
9. ~~**web_search.py 및 관련 코드 제거**~~ → ✅ agent/core.py·bridge.py·테스트 3개 파일 정리 완료 (2026-04-06)
10. ~~**SideMenuPanel + MemoryDB CRUD + AdminPanel + CharacterCreatePanel**~~ → ✅ 신규 구현 완료 (2026-04-06)
11. ~~**개선5 전체 (항목1~10)**~~ → ✅ 완료 (2026-04-09): 세계관 트리거·섹션 RAG·세션 world_id·메모리 정책·prompt_guides CRUD·가이드 주입·캐릭터 생성 연결·default 세계관·character_overrides. 35개 테스트 통과 (총 410개).
12. ~~**개선6 전체 (UI-1~UI-4·DB-1~DB-2·F-1)**~~ → ✅ 완료 (2026-04-10): 슬라이더 교체·토글 수정·오버플로·CRUD 스크롤·세계관 트리·WorldCreatePanel. 421개 테스트 통과.
13. ~~**대화품질 개선 (존댓말·나레이션·트리거·학습데이터)**~~ → ✅ 완료 (2026-04-10): 존댓말 지시 강화·CH_MookHyeon 신규·`**...**` 나레이션 분리·culture 트리거 과발동 수정·Seaside.md 소설체 재작성·build_sft 시스템 프롬프트 수정·로그 데이터 정리. 421개 테스트 통과.
14. ~~**MagicMock/ 디렉토리 생성 부작용**~~ → ✅ 완료 (2026-04-20): stub_agent / bridge 픽스처에 `agent.cfg = {}` 추가. .gitignore에 `MagicMock/` 추가. 474개 테스트 통과.
15. ~~**개선7~8 (캐릭터 확장 + 대화 품질)**~~ → ✅ 완료 (2026-04-20): mood_decay/mood_hold·summarizer 구조화·RAG 지시문·model_version·PIP bubbleDirection·CharacterBuildPanel·EmotionPanel. 474개 테스트 통과.
16. ~~**world_trigger.py 트리거 임계값 개선**~~ → ✅ 완료 (2026-04-20): 코사인 유사도 ≥ 0.55 단일 판정 → 절대 점수 ≥ 0.9 + 토큰 부분 매칭(0.5 가중치) 이중 판정으로 정밀도 향상.
17. ~~**reindexWorldKnowledge 자동 호출 완성**~~ → ✅ 완료 (2026-04-20): add(MemoryDBPanel) / update / delete / create(WorldCreatePanel) 모든 경로에서 자동 reindex. `_append_to_world_source` 데드 코드 제거.
18. ~~**메모리 3계층 구조 (단기→중기→장기)**~~ → ✅ 완료 (2026-04-24): `evict_to_context()` + `session_context` + `_layer_e()` + `character_notes` + `_PROMISE_RE` 감지. 즉시 flush + background thread 도입.
19. ~~**장기 메모리 VDB 품질 개선**~~ → ✅ 완료 (2026-04-24): cosine dedup 0.85 / quota 200 / TTL 30일 / 검색·저장 threshold 0.65 / 요약 산문화.
20. ~~**SIGTERM 수신 시 어댑터 저장 안 됨**~~ → ✅ 완료 (2026-04-24): `_sigterm_received` 플래그 + `SystemExit` 핸들러로 조기 종료 경로에서도 adapter 저장 보장.
21. ~~**조기 종료 시 eval 미실행**~~ → ✅ 완료 (2026-04-24): train_monitor가 프로세스 종료 후 eval 블록 직접 실행 (조기·정상 모두). lora_train에 `--skip_eval` 자동 주입으로 중복 방지.
22. **config.py adapter_path v12 업데이트 필요** → v12 학습 완료 후 `output/LoRA_v12/adapter`로 업데이트.
