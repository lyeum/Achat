# LoRA 학습 매뉴얼

> 환경: Ubuntu 24.04 WSL2 / RTX 5060 Ti (VRAM 8GB / RAM 8GB) / CUDA 12.8
> 모델: Qwen2.5-3B-Instruct
> 방식: bfloat16 풀 파라미터 + LoRA (BitsAndBytes 4-bit 미사용 — Blackwell SM 10.x 미지원)

---

## 0. 현재 학습 방식 구조

### 0-1. LoRA가 가중치를 다루는 방식

기존 베이스 모델(Qwen2.5-3B)의 **모든 파라미터는 완전히 동결(freeze)** 된다.
새로운 가중치를 직접 추가하는 대신, 각 대상 레이어에 **저랭크 행렬 쌍 A·B를 삽입**하여 그것만 학습한다.

```
[기존 레이어 — 완전 동결]
    W_original (예: q_proj, 2048×2048)
    requires_grad = False  ← 절대 바뀌지 않음

[LoRA 삽입 — 이것만 학습]
    W_A  shape: (r, in_dim)    r=16 이면 (16, 2048)
    W_B  shape: (out_dim, r)   r=16 이면 (2048, 16)
    requires_grad = True

[추론 시 실제 적용값]
    W_effective = W_original + (alpha/r) × (W_B @ W_A)
                              ↑ 스케일링: alpha=32, r=16 → ×2.0
```

`W_A`는 랜덤 가우시안으로 초기화, `W_B`는 0으로 초기화 → **학습 시작 시 LoRA 기여분은 0**이라 안정적.

---

### 0-2. 현재 학습 대상 레이어 (target_modules)

Transformer 블록의 Attention 4개 + FFN 3개, **전 28개 레이어에 걸쳐** 모두 적용.

| 모듈 | 역할 | 위치 |
|---|---|---|
| `q_proj` | Query 투영 | Attention |
| `k_proj` | Key 투영 | Attention |
| `v_proj` | Value 투영 | Attention |
| `o_proj` | Output 투영 | Attention |
| `gate_proj` | FFN 게이트 (SiLU) | Feed-Forward |
| `up_proj` | FFN 업 투영 | Feed-Forward |
| `down_proj` | FFN 다운 투영 | Feed-Forward |

Bias는 모두 동결(`bias="none"`). Embedding / LayerNorm 도 동결.

---

### 0-3. 파라미터 수

```
전체 모델:    3,090,000,000 파라미터  (동결)
LoRA 추가분:     ~30,000,000 파라미터  (학습)  ← 전체의 약 1%

계산:
  레이어당 LoRA 파라미터 = 7개 모듈 × 2 × (r × dim)
  예) r=16, dim=2048 → 7 × 2 × 16 × 2048 ≈ 458,752
  28 레이어 × 458,752 ≈ 12.8M  (단방향 기준 — 실제는 양방향 포함 ~30M)
```

---

### 0-4. 정밀도 및 메모리 관리

| 항목 | 설정 | 이유 |
|---|---|---|
| 모델 로드 정밀도 | **bfloat16** | RTX 5060 (Blackwell) 4-bit 양자화 미지원 |
| LoRA 가중치 정밀도 | bfloat16 (자동) | 모델과 동일 dtype 따라감 |
| Gradient 계산 | float32 누적 | 기울기 소실 방지 |
| Gradient Checkpointing | **활성** | Activation을 저장 않고 backward 시 재계산 → VRAM ~30% 절약 |
| `use_cache = False` | 강제 비활성 | KV 캐시와 gradient checkpointing 충돌 방지 |

---

### 0-5. Loss 계산 범위 (현재 방식의 한계)

```
[토크나이징 결과]
input_ids: [<system>...하루다</s>][<user>뭐해</s>][<assistant>그냥</s>]
labels:    [  동일  ............][  동일  .......][   동일  ........]
                ↑                      ↑                  ↑
            loss 계산됨           loss 계산됨         loss 계산됨
```

현재는 `labels = input_ids` 로 **전체 토큰에 대해 loss를 계산**한다.
시스템 프롬프트·유저 턴까지 loss에 포함되므로 모델이 "무엇을 예측해야 하는가"가 다소 모호해진다.

> **v7부터 적용**: assistant 토큰 구간만 loss 계산 (시스템·유저 구간 labels → -100 마스킹)
> → 응답 품질 직접 최적화, 과적합 억제 효과. `tokenize_dataset._mask_labels()` 구현 완료.

---

### 0-6. 옵티마이저 및 스케줄러

```
옵티마이저: AdamW
  - lr: 2e-4 (초기값)
  - weight_decay: 0.01 (L2 정규화)
  - beta1=0.9, beta2=0.999 (기본값)

스케줄러: Cosine Decay
  - warmup: 전체 스텝의 5% (loss 안정화 후 감쇠 시작)
  - 최종 lr → 0 으로 수렴

유효 배치:
  batch_size(1) × grad_accum(8) = 8
  → 8샘플 분의 gradient를 누적 후 1회 업데이트
```

---

### 0-7. Best Model 선택 방식

```
eval_split > 0 일 때:
  save_steps(100)마다 eval loss 계산
  → 가장 낮은 eval loss 시점의 가중치를 자동 선택
  → 학습 완료 후 adapter/ 에 best 가중치 저장

eval_split = 0 일 때:
  best 자동 선택 불가 → 마지막 epoch 가중치 저장
  (v3 실측: epoch 3~4가 best, 이후 eval loss 반등)
```

---

## 1. 학습 루프 내부 흐름

```
[데이터 로드]
training/data/**/*.jsonl (또는 data/lora/)
    — `_excluded/` 하위 파일은 자동 제외 (dataset.py load_jsonl_files)
    │
    ▼ load_training_data()
HuggingFace Dataset 객체 (text 필드)
    │
    ▼ tokenize_dataset()   — truncation=True, max_length=512
    │                        labels = _mask_labels(input_ids)
    │                        → assistant 응답 구간만 loss 계산 (v7~)
    │                        → 시스템·유저 토큰 구간은 labels=-100 마스킹
    │
    ▼ train_test_split()   — eval_split > 0 이면 90% train / 10% eval 분리
    │                        eval_split = 0 이면 전체 train, eval 없음
    │
[모델 로드]
AutoModelForCausalLM (bfloat16, device_map="auto")
    │
    ▼ get_peft_model()     — LoRA 어댑터 주입
    │                        target: q/k/v/o/gate/up/down proj (전 레이어)
    │                        trainable params: ~30M / 전체 3.1B
    │
[학습 루프 — HuggingFace Trainer]
    │
    ├─ 매 스텝:
    │   forward → loss 계산 → backward → gradient accumulation(8스텝)
    │   → optimizer step (AdamW + cosine LR decay)
    │
    ├─ logging_steps마다:  train loss / grad_norm / lr / epoch 출력
    │
    ├─ save_steps(100)마다:
    │   eval_split > 0 → eval loss 계산 → best이면 checkpoint 교체
    │   eval_split = 0 → checkpoint만 저장 (eval 없음)
    │
    └─ 학습 완료:
        load_best_model_at_end=True (eval 있을 때) → best checkpoint 복원
        model.save_pretrained() → output/{run_name}/adapter/ 저장
        checkpoint-* 삭제 (가중치·그래프·로그 제외)
        │
        ▼  --skip_eval 없을 때만
        VRAM 완전 해제
            trainer.model=None → del trainer / model / tokenizer
            gc.collect() → torch.cuda.empty_cache() + synchronize()
            여유 VRAM 로그 출력
        │
        ▼  subprocess (독립 프로세스, 각각 순차 실행)
        training/eval/ai_tell_checker.py --adapter {adapter_path}
        training/eval/memory_test.py     --adapter {adapter_path}
        training/eval/scenario_eval.py   --adapter {adapter_path}
```

**총 스텝 수 계산:**
```
총 스텝 = (샘플 수 × 0.9) ÷ (batch_size × grad_accum) × epochs
예 | 3170 × 0.9 ÷ (1 × 8) × 3 ≈ 1070 스텝  (v11 기준 — 3,170건, 3 epoch, r=32)
예 | 2150 × 0.9 ÷ (1 × 8) × 3 ≈ 726 스텝   (v10 기준 — 2,150건, 3 epoch, 권장)
예 | 2401 × 0.9 ÷ (1 × 8) × 3 ≈ 810 스텝   (v9 기준 — 2,401건, 3 epoch)
예 | 2401 × 0.9 ÷ (1 × 8) × 5 ≈ 1351 스텝  (v8 기준 — 2,401건, 5 epoch, 과적합 발생)
```

---

## 2. 학습 시작 방법

### 2-1. 사전 확인

```bash
# VRAM 여유 확인 (학습 전 잔류 프로세스 정리)
nvidia-smi

# 잔류 Python 프로세스 확인 및 정리
ps aux | grep lora_train | grep -v grep
kill <PID>

# 패키지 확인
uv run python -c "import peft, transformers, datasets; print('OK')"
```

### 2-2. 백그라운드 실행 (train_monitor 경유, 권장)

```bash
mkdir -p output/LoRA_vN  #FIX# 버전명 — 로그 파일 저장을 위해 먼저 생성
nohup uv run python training/train_monitor.py \
  -- python -m training.lora_train \
  --data_dir training/data \
  --output_dir output/LoRA_vN \  #FIX# 버전명 (예: LoRA_v9)
  --epochs 3 \                    #FIX# epoch 수 (v8 실측: 5 epoch에서 과적합, 3 권장)
  --max_samples -1 \
  --weight_decay 0.01 \
  --lora_dropout 0.1 \
  --eval_split 0.1 \
  --logging_steps 20 \
  > output/LoRA_vN/train.log 2>&1 &  #FIX# 버전명 일치
echo "PID: $!"

# 실시간 모니터링 (별도 터미널)
tail -f output/LoRA_vN/train.log
```

> train_monitor.py: 학습 프로세스를 subprocess로 실행하며 `trainer_state.json`을 폴링.
> eval_loss N회 연속 상승 또는 train/eval gap 초과 시 자동 SIGTERM → VRAM 해제.
> `--eval_split 0.1` 필수 (eval 없으면 과적합 감지 불가).

### 2-3. eval 없이 실행 (VRAM 부족 시)

```bash
mkdir -p output/LoRA_vN  #FIX# 버전명
nohup uv run python training/train_monitor.py \
  -- python -m training.lora_train \
  --data_dir training/data \
  --output_dir output/LoRA_vN \  #FIX# 버전명
  --epochs 3 \                    #FIX# epoch 수
  --max_samples -1 \
  --weight_decay 0.01 \
  --lora_dropout 0.1 \
  --eval_split 0 \
  --logging_steps 20 \
  > output/LoRA_vN/train.log 2>&1 &  #FIX# 버전명 일치
echo "PID: $!"
```

> eval 없으면 best checkpoint 자동 선택 불가 → v8 실측 기준 epoch 2~3이 best.
> train_monitor의 과적합 감지도 eval 없으면 동작 안 함 → `--eval_split 0.1` 권장.

### 2-4. EWC 다단계 학습 (v9~)

```bash
# Step 1: 이전 버전 Fisher 계산 (vN → vN+1 학습 전에 실행, ~5분 소요)
uv run python -m training.ewc \
  --adapter output/LoRA_vN/adapter \  #FIX# 이전 버전 어댑터 경로 (예: LoRA_v8)
  --out output/LoRA_vN \              #FIX# fisher.pt / ref_params.pt 저장 위치
  --n_samples 200
# → output/LoRA_vN/fisher.pt, output/LoRA_vN/ref_params.pt 생성
# (--model 기본값: Qwen/Qwen2.5-3B-Instruct, --data_dir 기본값: training/data)

# Step 2: train_monitor + EWC + 카테고리 가중치 적용 학습
mkdir -p output/LoRA_vN+1  #FIX# 버전명 (예: LoRA_v9)
nohup uv run python training/train_monitor.py \
  -- python -m training.lora_train \
  --data_dir training/data \
  --output_dir output/LoRA_vN+1 \     #FIX# 버전명
  --epochs 3 \                        #FIX# epoch 수 (3 권장 — v8 실측: 5는 과적합)
  --max_samples -1 \
  --weight_decay 0.01 \
  --lora_dropout 0.1 \
  --eval_split 0.1 \
  --logging_steps 20 \
  --ewc_fisher output/LoRA_vN/fisher.pt \      #FIX# Step 1 출력 경로
  --ewc_ref_params output/LoRA_vN/ref_params.pt \  #FIX#
  --ewc_lambda 500 \
  --category_weights '{"emotion": 2.0, "long_dialogue": 1.5}' \
  > output/LoRA_vN+1/train.log 2>&1 &  #FIX# 버전명 일치
echo "PID: $!"

# 실시간 로그 확인
tail -f output/LoRA_vN+1/train.log

# VRAM 모니터링 (별도 터미널)
watch -n 2 nvidia-smi
```

> EWC 없이 처음부터 학습하는 경우 `--ewc_fisher/--ewc_ref_params/--ewc_lambda` 생략.
> `--category_weights` 는 EWC 없이도 독립적으로 사용 가능.
> `--ewc_lambda` 권장 시작값: **500** (v9 실험 기준). 너무 낮으면(0.5 등) EWC 효과 미미.
> train_monitor는 eval_loss 연속 상승 또는 train/eval gap 초과 시 자동 조기 종료. `--eval_split 0.1` 필수.

### 2-5. CPU 파이프라인 테스트 (동작 확인용)

```bash
uv run python training/lora_train.py \
  --model Qwen/Qwen2.5-3B-Instruct \
  --data_dir training/data \
  --no_save \
  --max_steps 5 \
  --max_length 128 \
  --logging_steps 1
```

> CPU 모드: Qwen2.5-3B float32 로드에 RAM ~12GB 필요.
> 부족 시 `--model Qwen/Qwen2.5-1.5B-Instruct` 사용.

---

## 3. 실시간 모니터링

tqdm 출력은 `\r` 덮어쓰기 방식이라 로그 파일에서 grep이 안 됨.
**checkpoint의 trainer_state.json을 직접 읽는 방식** 사용 (100 스텝마다 갱신).

### 학습 속도 패턴 (정상 동작)

tqdm의 `s/it` 값이 학습 도중 급등했다가 점점 줄어드는 패턴이 반복된다. 이는 버그가 아니라
**eval 실행 시 gradient_checkpointing이 일시 비활성화**되기 때문이다.

```
정상 패턴 예시 (v9 기준):
  초반     5~6s/it   ← CUDA warm-up 구간, 짧은 시퀀스 배치
  급등    90s/it    ← eval 실행 (save_steps=100 도달)
             gradient_checkpointing=False로 전환 → full activation VRAM 점유
  회복    66→50→38→24→17s/it  ← train 모드 재전환 후 점진적 복구
  안정    12~17s/it  ← 이후 정상 속도
```

- eval이 끝날 때마다 이 패턴이 반복된다 (`save_steps=100`마다 1회).
- EWC 적용 시 패널티 계산이 매 스텝에 추가되어 비EWC 학습보다 전반적으로 느리다.
  - v8 (EWC 없음): ~9s/it / v9 (EWC λ=500): ~14s/it

```bash
# 현재 스텝당 속도 추이 확인
grep -o '[0-9]*\.[0-9]*s/it' output/LoRA_vN/train.log | tail -20  #FIX# 버전명
```

### loss 그래프 (자동 생성)

학습 중 **epoch 종료마다** + **전체 학습 완료 시** 자동으로 PNG 저장됨:

```
output/LoRA_vN/            #FIX# 버전명
├─ loss_curve_epoch01.png   ← epoch 1 종료 시
├─ loss_curve_epoch02.png   ← epoch 2 종료 시
├─ ...
└─ loss_curve_final.png     ← 전체 완료 시 (train + eval 통합)
```

- train loss: 파란 실선
- eval loss: 빨간 실선 + 원 마커 (eval_split > 0 일 때만)
- best eval 지점: 점선 수직선 표시

### 진행도 + train loss (checkpoint 기반)

```bash
ls -d output/LoRA_vN/checkpoint-* 2>/dev/null | sort -t- -k2 -n | tail -1 | \  #FIX# 버전명
xargs -I{} python3 -c "
import json, sys
s = json.load(open('{}/trainer_state.json'))
logs = [l for l in s['log_history'] if 'loss' in l and 'eval' not in str(l)]
print(f'진행: {s[\"global_step\"]} / {s.get(\"max_steps\",\"?\")} step')
print(f'best eval loss: {s.get(\"best_metric\", \"없음 (eval 비활성)\")}')
print()
for l in logs[-5:]:
    print(f'  step {l[\"step\"]:>4}  epoch {l[\"epoch\"]:.2f}  loss {l[\"loss\"]}')
"
```

### tqdm 진행바 (현재 step 위치)

```bash
python3 -c "
import re
c = open('output/LoRA_vN/train.log', 'rb').read().decode('utf-8', errors='ignore')  #FIX# 버전명
m = re.findall(r'\d+/\d+ \[\d+:\d+<\d+:\d+,\s+[\d.]+s/it\]', c)
print(m[-1] if m else 'loading...')
"
```

### 학습 완료 확인

```bash
tail -5 output/LoRA_vN/train.log  #FIX# 버전명
# "어댑터 저장 완료" 메시지 확인
```

### VRAM 모니터링 (별도 터미널)

```bash
watch -n 2 nvidia-smi
```

> 학습 중 정상 점유: ~7,200MiB (VRAM 8GB 기준 ~88%).
> 8,151MiB 한계치 초과 시 OOM → `--max_length 256` 또는 `--grad_accum 16` 으로 조정.

---

## 4. 인자 전체 목록

| 인자 | 기본값 | 설명 |
|---|---|---|
| `--model` | `Qwen/Qwen2.5-3B-Instruct` | HuggingFace 모델명 |
| `--data_dir` | `training/data` | 학습 데이터 루트 디렉토리 |
| `--output_dir` | `output/LoRA_v7` | 체크포인트 및 어댑터 저장 경로 |
| `--subset` | `None` (전체) | `conversation` / `function` / `None`(전체 혼합) |
| `--epochs` | `3` | 학습 에폭 수 |
| `--batch_size` | `1` | 디바이스당 배치 크기 |
| `--grad_accum` | `8` | Gradient accumulation 스텝 수 (유효 배치 = batch×grad_accum) |
| `--lr` | `2e-4` | 초기 학습률 (cosine decay 적용) |
| `--max_length` | `512` | 최대 시퀀스 길이 (초과 시 잘림) |
| `--lora_r` | `16` | LoRA rank (높을수록 파라미터 많음, 표현력 증가. v11: `32` 사용) |
| `--lora_alpha` | `32` | LoRA alpha (스케일링 = alpha/r, 통상 r의 2배. v11: `64` 사용) |
| `--lora_dropout` | `0.05` | LoRA dropout 비율 (과적합 억제 시 `0.1` 권장) |
| `--weight_decay` | `0.0` | L2 정규화 강도 (과적합 억제 시 `0.01` 권장) |
| `--eval_split` | `0.1` | 검증셋 비율 (`0` 이면 eval 완전 비활성) |
| `--max_samples` | `-1` (전체) | 학습에 사용할 최대 샘플 수 (shuffle 후 선택) |
| `--save_steps` | `100` | 체크포인트 저장 및 eval 실행 주기 (스텝 단위) |
| `--logging_steps` | `10` | train loss 출력 주기 (스텝 단위) |
| `--max_steps` | `-1` (에폭 기준) | 양수 설정 시 해당 스텝 수에서 강제 종료 |
| `--no_save` | `False` | 학습 후 어댑터 저장 건너뜀 (파이프라인 테스트용, 자동 평가도 실행 안 함) |
| `--skip_eval` | `False` | 학습 완료 후 자동 평가(`training/eval/`) 건너뜀 (VRAM 해제도 생략됨) |
| `--ewc_fisher` | `None` | `fisher.pt` 경로 — `--ewc_lambda > 0` 이면 필수 |
| `--ewc_ref_params` | `None` | `ref_params.pt` 경로 — `--ewc_lambda > 0` 이면 필수 |
| `--ewc_lambda` | `0.0` | EWC 강도 (`0` 이면 비활성, 권장 시작값 `500`) |
| `--category_weights` | `None` | 카테고리별 가중치 JSON 문자열 (예: `'{"emotion": 2.0, "long_dialogue": 1.5}'`) |

---

## 5. 과적합 분석 및 대응

### 학습 루프의 과적합 유발 요소

| 요소 | 현재 기본값 | 영향 | 권장 |
|---|---|---|---|
| `weight_decay` | `0.0` | L2 정규화 없음 → 가중치 무제한 성장 | `0.01` |
| `lora_dropout` | `0.05` | 소규모 데이터에서 너무 낮음 | `0.1` |
| `labels = _mask_labels` | assistant 토큰만 loss (v7~) | 전체 토큰보다 loss 값 자체가 높게 나옴 — v6 이전과 직접 비교 불가 | ✅ v7 적용 완료 |
| 샘플 수 3,170건 | 고정 (v11) | 적은 데이터 × 많은 epoch = 빠른 암기 | `--max_samples` 로 조절 |

> ⚠️ **v7부터 loss 스케일이 달라짐**: assistant 토큰만 계산하므로 같은 품질이어도 v7 loss 값이 v1~v6보다 높게 나온다.
> v7 이후 버전끼리만 loss 값으로 비교할 것.

**실제 관측 (lora_haru_v3, 30 epoch) — 전체 토큰 loss 기준:**
```
epoch  3   eval 0.7226  ← best
epoch  5   eval 0.79    ↑ 과적합 시작
epoch 30   train 0.07 / eval 1.02  ↑ 완전 암기
```

**실제 관측 (LoRA_v6, 10 epoch) — 전체 토큰 loss 기준:**
```
epoch ~5   eval 0.7198  ← best  (v3 대비 개선)
epoch 10   train 0.08  ↑ 과적합 진행
```

**실제 관측 (LoRA_v7, 4 epoch, max_samples=1200) — assistant 토큰 loss 기준 ⚠️:**
```
epoch ~1.5  eval 1.714  ← 초기 수렴
epoch ~2.2  eval 1.760  ↑ 반등 시작 (과적합 조기 진입)
epoch ~3    eval 1.687  ← best
epoch ~3.7  eval 1.822  ↑ 과적합 진행
train  4    loss 0.362    (train/eval gap 1.33 — 과적합 심함)
```
→ 원인: max_samples=1200 (train 1,080건) + assistant 마스킹으로 샘플당 유효 토큰 감소 → 조기 암기
→ **v8 대응: max_samples=-1 (전체 2,401건, emotion+long_dialogue 추가), 5 epoch**

**실제 관측 (LoRA_v8, 5 epoch, 2,401건, category_weights emotion×2/long_dialogue×1.5) — assistant 토큰 loss 기준 ⚠️:**
```
step  900  eval 1.353  ← best (epoch 2.7)
step 1000  eval 1.448  ↑ 과적합 시작
step 1470  train 0.124 / eval 1.503  ↑ 완전 과적합 (gap=1.38 > threshold 1.2)
memory_test 3/5 (60%) — 이름 recall 실패 원인: 훈련 데이터 system prompt "너는 하루다." 오염 (234건)
```
→ 원인: 5 epoch 과다 + system prompt 이름 고정 오염 → 모델이 이름="하루" 암기
→ **v9 대응: 3 epoch, system prompt "너는 하루다." 제거 (emotion 180건 + long_dialogue 54건), EWC(λ=500)**

**실제 관측 (LoRA_v9, 3 epoch, 2,401건, EWC λ=500, category_weights emotion×2/long_dialogue×1.5) — assistant 토큰 loss 기준 ⚠️:**
```
step  600  eval 1.511  ← best (epoch 1.70)
step  700  eval 1.513  ← 거의 유지
step  800  eval 1.550  ↑ 소폭 상승
step  882  eval 1.548  ← 최종 (3 epoch 완료)
train loss 최종: 0.688
ai_tell 0건/10개 응답 — 유지
memory_test 4/5 (80%) — 이름 기억 1건 실패, 직업/감정/선호/계획 통과
```
→ EWC 패널티 영향으로 v8 best(1.353)보다 높은 eval_loss — 단, 이는 정규화 효과이며 memory_test 개선(3/5→4/5)으로 품질 향상 확인.
→ 이름 기억 실패 잔존: 캐릭터 이름은 system prompt에서 런타임에 주입되므로 학습 데이터에 고정할 필요 없음.

**실제 관측 (LoRA_v10, 3 epoch, 2,150건, EWC 미적용, character_schema 기반) — assistant 토큰 loss 기준 ⚠️:**
```
eval best 1.512  (3 epoch 완료)
EWC 미적용: v9 어댑터 기반 연속 학습 대신 풀리트레인 방식으로 전환
character_schema.yaml 기반 {char_name} 플레이스홀더 시스템 프롬프트 처음 적용
학습-추론 프롬프트 형식 정합성 확보
```
- 데이터: 2,150건 (personality 2개·speech_style formal/mixed 3개 제거, long_dialogue 4개 신규 추가)
- 시스템 프롬프트 형식: `너는 {char_name}이다.` 플레이스홀더 + 카테고리 행동 속성만 포함
- 학습-추론 프롬프트 형식 정합성 확보 (character_schema.yaml ↔ rewrite_system_prompts.py ↔ CH_Haru.yaml 동기화 완료)
- 실제 스텝: 2,150 × 0.9 ÷ 8 × 3 ≈ **726 스텝**

**v11 학습 데이터 준비 완료 (2026-04-14):**
- 데이터: **3,170건, 57파일** (+1,020건, +19파일 vs v10)
- 신규 카테고리:
  - `affection/formal/` — 존댓말 캐릭터 친밀도 6단계 (각 30건, 계 180건)
  - `common/world_trigger_response` — 세계관 위치/배경 반응 (50건)
  - `emotion/transition` — 대화 중 자연스러운 감정 전환 멀티턴 (40건)
  - `personality/cynical` — 냉소적 현실주의 (50건, 서술 개선)
  - `personality/tsundere` — 츤데레 감정 표현 패턴 (50건, 서술 개선)
  - `personality/energetic` — 활발하고 주도적 (신규)
  - `personality/melancholic` — 감수성 깊고 내면적 (신규)
  - `personality/warm` — 따뜻하고 다정 (신규)
- 기존 확장: `emotion/` 8종 각 20→40건, `long_dialogue/daily_chat` 21→51건
- 시스템 프롬프트 동기화: `rewrite_system_prompts.py` 57파일 전체 재처리 완료 (오류 0)
- `rewrite_system_prompts.py` `_FORMAL_KEYS` — `affection/formal/*` 6종 추가 (FORMAL_BASE 적용)
- category_weights 권장: `emotion×2 / long_dialogue×1.5 / personality×1.5 / affection/formal×1.5`
- 예상 스텝: 3,170 × 0.9 ÷ 8 × 3 ≈ **1,070 스텝**

**실제 관측 (LoRA_v11, EWC 없음 풀리트레인, r=32/alpha=64, 3,170건) — assistant 토큰 loss 기준 ⚠️:**
```
step  700  eval 1.5387  ← best (epoch 1.97)  ✅ adapter/ 에 복사
step  800  eval 1.5412  ↑ 소폭 상승
step  900  학습 수동 중단 (epoch ~2.5)
train loss 최종(step 880): 약 0.9x
best checkpoint: checkpoint-700 → output/LoRA_v11/adapter/ 복사
```
→ EWC 없이 전체 재학습 (v10 데이터가 v11에 포함되므로 EWC 불필요).
→ r=32/alpha=64: r=16 대비 파라미터 2배. 3B 모델 추론 시 병합(merge_lora) 후 오버헤드 없음.
→ train_monitor 버그 수정: `find_latest_state()` 추가 — 학습 중 checkpoint-*/trainer_state.json 탐색.
  (이전 버전은 output_dir/ 루트만 폴링 → 학습 중 파일 없어 조기 종료 감지 불가였음)

### 샘플링 동작 방식

`--max_samples N` 지정 시 **파일별 비율 유지 (stratified)** 방식으로 추출.

```
전체 데이터 로드 (57개 파일, 3,170건)  ← v11 기준; v10까지는 2,150건
    │
    ▼  파일별 quota = ceil(N × 파일건수 / 전체건수)
    │
    ▼  각 파일 내 shuffle(seed=42).select(quota)
    │
    ▼  전체 합산 shuffle(seed=42)
    │
    ├─ 90%  train_ds
    └─ 10%  eval_ds
```

> 파일별 비율이 유지되므로 특정 카테고리가 과소/과대 대표되는 문제 없음.

---

## 6. 학습 결과 평가

> **자동 실행 (기본값)**: 학습 완료 후 VRAM 해제 → `ai_tell_checker.py` + `memory_test.py` 자동 실행됨.
> `--skip_eval` 또는 `--no_save` 시 건너뜀.
>
> **수동 실행**: 아래 명령어로 개별 실행 가능. `training/eval/` 경로 사용.

```bash
# AI-tell 표현 감소 확인 (베이스 모델 비교 포함)
uv run python training/eval/ai_tell_checker.py \
  --model Qwen/Qwen2.5-3B-Instruct \
  --adapter output/LoRA_vN/adapter \  #FIX# 어댑터 경로
  --compare_base

# 기억 유지 정확도 (목표: 5케이스 중 4건 이상)
uv run python training/eval/memory_test.py \
  --model Qwen/Qwen2.5-3B-Instruct \
  --adapter output/LoRA_vN/adapter    #FIX# 어댑터 경로

# 추론 속도 (목표: 8+ tok/s) — 수동 실행만
uv run python training/eval/speed_bench.py \
  --backend transformers \
  --model Qwen/Qwen2.5-3B-Instruct \
  --adapter output/LoRA_vN/adapter    #FIX# 어댑터 경로

# Phase 2/3 실환경 검증 (12턴 자동 대화) — 수동 실행만, LLM 풀 로드 필요
uv run python training/eval/verify_phases.py \
  --adapter output/LoRA_vN/adapter    #FIX# 어댑터 경로
```

> 어댑터 경로: eval_split > 0 이면 `{output_dir}/adapter/` 디렉토리,
> eval_split = 0 이면 특정 `checkpoint-N` 디렉토리 직접 지정.

결과는 `docs/학습후보.md` 섹션 4-3 양식에 기록.

---

## 7. 학습 개선안

> 구현 계획: [docs/plan/training_개선.md](../docs/plan/training_개선.md)

### 7-0. 학습 데이터 ↔ 추론 시스템 프롬프트 정합성 ✅ 구현 완료 (2026-04-02)

**문제:**
- 학습 데이터 system prompt: `너는 하루이다. [범용 제약 5문장]\n[카테고리 속성 1문장]`
- 추론 시 system prompt: `너는 {name}이다. {description} {speech} {personality} {affection[tier]} {emotion[mood]} {conv_hints} {rules}`
- 구조 불일치 → 모델이 학습한 system prompt 형태와 추론 시 입력이 달라 일관성 저하

**해결:**
- `character_schema.yaml`: 캐릭터 필드 계약. 슬롯 = 학습 카테고리 어휘.
- `CH_Haru.yaml`: `speech`/`affection`/`emotion`/`personality` 슬롯 체계로 재작성.
- `prompt_build.py _layer_a()`: YAML 슬롯 기반 조립 + preset 해석.
- `training/scripts/rewrite_system_prompts.py`: CATEGORY_ATTR 텍스트 = CH_Haru.yaml 슬롯 값과 정확히 동기화.
- 학습 데이터 system prompt 형식: `너는 {char_name}이다. [범용 제약]\n[카테고리 행동 속성]` — 이름 플레이스홀더화.

**효과:** 학습-추론 형식 정합. 신규 캐릭터는 YAML 슬롯만 채우면 학습 데이터와 자동 정렬됨.

---

### 7-1. Assistant 토큰 마스킹 (Loss 범위 개선)

**현재 문제:**
`labels = input_ids`로 설정되어 있어 시스템 프롬프트·유저 턴 토큰에도 loss가 계산됨.
모델이 "응답"뿐 아니라 "입력받는 방식"까지 암기하게 되어 과적합을 가속시킴.

**개선 방향:**
assistant 응답 구간 토큰만 labels를 살리고, 나머지는 `-100`으로 마스킹.

```
현재:  [<system>...][<user>...][<assistant>그냥]
loss:       ✓              ✓           ✓

개선 후: [<system>...][<user>...][<assistant>그냥]
loss:      -100          -100          ✓
```

기대 효과: 과적합 억제 + 응답 품질 직접 최적화 + eval loss 곡선 안정화

---

### 7-2. EWC 기반 다단계 연속 학습 ✅ 구현 완료 (`training/ewc.py`)

**현재 문제:**
매 버전을 처음부터 학습 — 이전 버전이 잘 배운 패턴도 새 데이터에 의해 덮어쓰여질 수 있음.
특히 log 데이터 추가 학습 시 기존 speech_style 패턴이 흐트러질 위험이 있음.

**개선 방향:**
EWC(Elastic Weight Consolidation)를 다단계 학습에 적용.

```
[vN 학습 완료]
    ↓
Fisher 계산  →  output/LoRA_vN/fisher.pt
                output/LoRA_vN/ref_params.pt
    ↓
eval 테스트  →  카테고리별 약점 파악
    ↓
[vN+1 학습]
    L_total = L_CE + λ/2 × Σ F_i × (θ_i - θ*_i)²
              ↑ 새 데이터    ↑ 이전 버전 보호 패널티
```

**Fisher Information:**
LoRA 파라미터(~30M)에 대해서만 `grad²` 평균으로 추정.
중요도 높은 파라미터(= 이전 학습에서 자주 업데이트된 파라미터)일수록 새 학습에서 더 강하게 고정.

**λ 튜닝 기준:**

| λ | 효과 |
|---|---|
| 0.1~1.0 | 효과 미미 — EWC 패널티가 학습 loss에 묻힘 |
| **500** | **권장 시작값 (v9 실험 기준)** |
| 1000+ | 강한 보호, 새 적응 제한 — 특정 카테고리만 추가할 때 |

---

### 7-3. 카테고리 가중치 샘플링 ✅ 구현 완료 (`training/dataset.py category_weights`)

**현재 문제:**
파일별 비율 고정 stratified sampling — 테스트에서 약한 카테고리를 발견해도 해당 부분만 집중 학습 불가.

**개선 방향:**
JSONL 레코드의 `category` 필드 기준으로 카테고리별 오버샘플링 배율 지정.

```
--category_weights '{"affection": 2.0, "memory_ref": 1.5}'

동작:
  affection 카테고리 → 전체 샘플의 2배 비율로 추출 (반복 허용)
  memory_ref 카테고리 → 1.5배 비율로 추출
  나머지 카테고리 → 기존 비율 유지
```

EWC와 조합 시 활용법:
- EWC로 이전 강점 보호하면서 약한 카테고리만 집중 학습
- λ 높게 설정 + category_weights 높게 설정 → 특정 카테고리 핀포인트 개선

---

### 7-4. KL Divergence 규제 (미래 후보)

> 현재 보류. DPO 파이프라인 구축 이후 검토.

- `L_total = L_CE + β × KL(π_new || π_ref)`
- EWC와 달리 파라미터 공간이 아닌 **출력 분포 공간**에서 규제
- 학습 중 reference 모델의 forward pass가 추가로 필요 → VRAM 증가
- RLHF/DPO 방향으로 확장할 때 적합

---

### 7-5. VRAM 절감 기법 (현재 환경 기준)

> 현재 환경: RTX 5060 Ti VRAM 8GB / BitsAndBytes 4-bit 미지원(Blackwell SM 10.x) / 학습 중 VRAM 점유 ~96%.
> BnB 4-bit 모델 로드는 불가능하지만, **옵티마이저 상태**와 **어텐션 연산** 쪽에서 별도로 절감 가능.

#### ① 8-bit AdamW (옵티마이저 상태 절감)

```
현재 bfloat16 AdamW 옵티마이저 상태:
  파라미터 1개당 → m(1st moment) + v(2nd moment) = 2 × float32 = 8 bytes
  LoRA 30M 파라미터 기준 → 약 240 MB

8-bit AdamW 적용 시:
  m, v를 8-bit로 압축 → float32 대비 약 75% 절감 → 약 60 MB
```

bitsandbytes의 **4-bit 모델 로드**는 Blackwell 미지원이지만,
**옵티마이저 8-bit**는 모델 가중치 양자화가 아니므로 별도 동작 가능성 있음.

```python
# TrainingArguments에서 optim 변경만으로 적용
optim="adamw_bnb_8bit"   # 현재: "adamw_torch"
# 또는
optim="paged_adamw_8bit" # VRAM 부족 시 optimizer state를 CPU RAM으로 page-out
```

> ⚠️ Blackwell에서 bnb optimizer도 동작 여부 사전 확인 필요.
> `paged_adamw_8bit`는 CPU RAM(16GB 기준 여유 있음)으로 overflow → 안전한 폴백.

---

#### ② Flash Attention 2 (어텐션 연산 절감)

기존 어텐션은 시퀀스 길이 N에 대해 N² 크기의 attention matrix를 VRAM에 유지.
Flash Attention 2는 이를 **블록 단위(block-wise)로 타일링**하여 VRAM에 전체를 올리지 않고 처리.

```
기존 Attention:
  Q·K^T 행렬 (N × N) → VRAM에 상주
  N=512, bfloat16 기준 → 약 512KB / 레이어 → 28레이어 × ~14 MB

Flash Attention 2:
  블록 크기(BLOCK_M, BLOCK_N)로 나눠 SRAM에서 처리
  → attention matrix 전체를 VRAM에 올리지 않음 → ~30-40% 절감
```

```python
# AutoModelForCausalLM.from_pretrained에 추가
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    dtype=torch.bfloat16,
    attn_implementation="flash_attention_2",  # 추가
    ...
)
```

```bash
pip install flash-attn --no-build-isolation
```

> Qwen2.5는 Flash Attention 2 공식 지원. Blackwell CUDA 12.8 환경에서 동작 확인 필요.
> 적용 시 추가 설치 없이 `attn_implementation` 파라미터 하나로 전환 가능 — 우선순위 ①.

---

#### ③ GaLore (Gradient Low-Rank Projection)

LoRA는 **가중치**에 저랭크 행렬을 추가하는 방식. GaLore는 **그래디언트**를 저랭크 부분공간에 투영.

```
LoRA 방식:
  W_eff = W_frozen + B·A   ← A, B 파라미터 VRAM 점유
  전체 파라미터 그래디언트 계산 → LoRA 파라미터만 업데이트

GaLore 방식:
  W_full 전체 그래디언트 계산
  → SVD로 저랭크 부분공간 추출 (주기적 갱신, 예: 200 step마다)
  → 압축된 그래디언트만 옵티마이저에 전달
  → 추가 파라미터 없음, 기존 가중치 직접 업데이트
```

| 항목 | LoRA (rank=16) | GaLore (rank=16) |
|---|---|---|
| 추가 파라미터 | ~30M (VRAM) | 0 (VRAM 없음) |
| 옵티마이저 상태 | LoRA 파라미터 기준 | 투영된 gradient 기준 (더 작음) |
| 표현력 | 제한적 (삽입 위치 고정) | 전체 가중치 업데이트 가능 |
| 구현 난이도 | 낮음 (PEFT 라이브러리) | 중간 (별도 설치 필요) |

```bash
pip install galore-torch
```

> 소규모 SFT 파인튜닝에서 LoRA 대비 동등하거나 더 나은 품질로 VRAM 절감.
> 단, step마다 SVD 계산 비용이 있어 학습 속도가 소폭 저하될 수 있음.

---

#### ④ torch.compile (처리량 개선)

VRAM 절감은 아니지만 같은 VRAM에서 더 빠른 처리 → 동일 시간 내 더 많은 학습.

```python
# Trainer 생성 전 모델에 적용
model = torch.compile(model)
```

PyTorch 2.x의 JIT 컴파일. 반복적인 연산 패턴(학습 루프)에서 **10~25% 처리량 향상**.
단, 첫 forward pass에 컴파일 시간(수십 초) 소요. `--max_steps 5` 테스트에서는 오히려 느림.

---

### 7-6. 스케줄러 전략

현재 **Cosine Decay + 5% warmup** 사용. 대안 비교:

| 스케줄러 | 특징 | 적합한 상황 |
|---|---|---|
| **cosine** (현재) | 부드러운 감쇠, 안정적 | 일반 SFT |
| **WSD** (Warmup-Stable-Decay) | warmup → 일정 lr 유지 → 선형 감쇠 | 도메인 적응, 카테고리 균형 학습 |
| **linear** | 선형 감쇠, 단순 | 데이터 적고 epoch 적을 때 |
| **constant_with_warmup** | warmup 후 lr 고정 | 짧은 테스트 런 / step 수 불확실할 때 |
| **inverse_sqrt** | 초반 빠른 하강 후 완만 | 대용량 데이터, 장시간 학습 |

**WSD (Warmup-Stable-Decay) 권장 이유:**

```
cosine:
  lr │  ╮          ← warmup 후 바로 감쇠 시작
     │   ╰─────╮
     └──────────── step

WSD:
  lr │  ╮──────────╮   ← stable 구간에서 카테고리 전체를 고르게 학습
     │              ╲
     └──────────────── step
```

도메인 특화 SFT(캐릭터 말투, 감정 반응 등 카테고리가 다양할 때)에서
stable 구간이 충분히 확보되면 **모든 카테고리를 고르게 수렴**시키는 효과.

```python
# TrainingArguments
lr_scheduler_type="warmup_stable_decay",  # transformers 4.46+ 지원
warmup_ratio=0.05,
# stable 구간 끝 비율은 warmup_stable_decay_ratio 파라미터로 조정 (기본 0.9)
```

> transformers 버전이 낮으면 미지원. `constant_with_warmup` + 수동 step 지정으로 근사 가능.

---

### 7-7. 데이터 구성 전략 — 실측 기반 권장 설정

`--max_samples -1` + `training/data` 카테고리 비율 그대로 사용 시 가장 안정적인 대화 품질이 확인됨.

**이유:**

```
training/data/ 카테고리 구성
  affection/   (9파일)  ← 친밀도 단계별 반응 — 양이 가장 많음
  personality/ (5파일)  ← 성격 유형
  speech_style/(7파일)  ← 말투 조합
  common/      (3파일)  ← memory_ref / ai_tell_removal / persona_follow

stratified sampling이 이 비율을 유지 → 어느 한 카테고리가 과대표되지 않음
→ affection(친밀도) 변화에 따른 말투 변화가 자연스럽게 학습됨
```

**권장 설정 조합:**

```bash
--max_samples -1      # 전체 2,401건 사용 (emotion + long_dialogue 추가 후)
--data_dir training/data  # 카테고리 구조 그대로
--epochs 3            # v8 실측: 5 epoch에서 과적합. 3 권장
--eval_split 0.1      # best checkpoint 자동 선택 (VRAM 여유 있을 때)
--weight_decay 0.01   # 과적합 억제
--lora_dropout 0.1    # 소규모 데이터 기준
```

> `--max_samples` 를 1,200처럼 줄이면 유효 토큰 수 감소 + 조기 암기 → v7에서 실측 확인.
> 전체 데이터(-1)를 카테고리 비율 유지로 쓰는 것이 현 환경에서 최적.

---

### 7-8. 캐릭터 챗봇 파인튜닝 — 실무 접근법

동일 과제(특정 페르소나/말투/감정 반응 특화 소규모 SFT)에서 실무에서 자주 선택하는 방식:

#### ① SFT → DPO 2단계 (가장 일반적)

```
[1단계] SFT (현재 방식)
  고품질 데이터 → 기본 말투/페르소나/감정 반응 주입
  목표: loss 수렴, 캐릭터 붕괴 없음

[2단계] DPO (Direct Preference Optimization)
  (chosen, rejected) 쌍 데이터 준비
    chosen: 캐릭터에 맞는 이상적 응답
    rejected: AI투 표현 / 캐릭터 이탈 응답
  → 선호/거부 학습으로 미세 조정
  → reference 모델(SFT 완료본)과 비교하며 β × KL 규제
```

DPO는 RLHF 없이 preference 학습 가능. SFT 어댑터 기반으로 추가 학습이라 VRAM 부담 낮음.

#### ② ORPO (SFT + 선호 학습 1단계 통합)

```python
# trl 라이브러리
from trl import ORPOTrainer

# SFT loss + odds ratio preference loss를 동시에 계산
# reference 모델 forward pass 불필요 → DPO 대비 VRAM ~절반
# 단, 데이터 형식이 (prompt, chosen, rejected) 3항으로 구성돼야 함
```

DPO보다 심플하고 VRAM 효율적. 소규모 단일 GPU 환경에 적합.
캐릭터 챗봇 과제에서 "AI투 표현 줄이기" 같은 명확한 거부 쌍이 있을 때 효과적.

#### ③ 데이터 품질 원칙

실무에서 캐릭터 SFT에 공통으로 적용하는 원칙:

| 원칙 | 설명 |
|---|---|
| **고품질 > 고수량** | 정제된 500건 > 반복·노이즈 많은 5,000건 |
| **카테고리 균형** | 특정 패턴 과대표 시 모델이 해당 패턴만 반복 → stratified 필수 |
| **부정 예시 포함** | AI투 표현 제거 데이터(ai_tell_removal.jsonl)가 positive만큼 중요 |
| **말투 일관성** | 동일 캐릭터 내 speech_style 혼재 시 붕괴 발생 — 캐릭터별 분리 학습 권장 |
| **짧은 응답 다양화** | 단답형 응답이 70%+ → 길이 다양성 부족 시 모델이 단답만 생성 |

#### ④ 단계별 학습 (Curriculum Learning 변형)

```
[1라운드] 일반 말투 + 기본 성격 데이터 (speech_style/ + personality/)
    epoch 3~4 → best 수렴 확인
    ↓
[2라운드] 친밀도 변화 + 감정 반응 데이터 (affection/ + common/)
    EWC로 1라운드 패턴 보호하면서 추가 학습
    ↓
[3라운드] 실대화 로그 (training/log/ 수집 데이터)
    실사용 분포에 맞게 미세 보정
```

각 라운드에서 eval 결과를 보고 다음 라운드 데이터 선택 — 7-2(EWC) + 7-3(카테고리 가중치)와 조합.

> 현재 구조(training/data 카테고리 분리)가 이 방식을 바로 적용할 수 있는 형태로 이미 갖춰져 있음.

---

### 7-8-E. LoRA 학습 대상 영역 원칙

모든 기능에 LoRA를 적용하는 것은 학습 비용 대비 효율이 낮다.
아래 원칙에 따라 LoRA 투자 영역을 결정한다.

| 영역 | LoRA 필요성 | 이유 |
|---|---|---|
| **대화 (캐릭터 말투)** | **높음** | 반말 일관성·단답형·감정 억제는 프롬프트만으로 유지 어려움 |
| **나레이션** | 중간 | few-shot 프롬프트로 1차 제어 가능. 품질 부족 확인 시 추가 고려 |
| **기능 모드 파라미터 추출** | **낮음** | greedy decoding + repetition_penalty 조합으로 현재 충분 |
| **기능 결과 요약/응답** | **낮음** | Layer F 프롬프트 주입으로 대응. 3B 한국어 요약은 프롬프트 튜닝으로 보완 |
| **prompt_convert 변환** | **낮음** | 이미지 모델 특화 지식은 크롤링+프롬프트 파이프라인이 더 효과적 |

**프롬프트 엔지니어링 우선 적용 원칙:**

LoRA를 추가하기 전 반드시 프롬프트 단계에서 먼저 시도해야 한다.

```
[품질 개선 우선순위]
1. 프롬프트 구조 개선 (system_prompt 내용 정교화)
2. few-shot 예시 삽입 (나레이션·요약 등 문체 제어)
3. 위 두 단계로 부족할 때 → LoRA 학습 데이터 수집 및 추가 학습
```

이 원칙은 배포 환경(llama_cpp)에서 런타임 어댑터 교체가 불가능한 제약과도 연결된다.
프롬프트 방식은 전환 비용 0, 모델 재로딩 없음.

> 상세 배경: [docs/BUG/BUG_03.md](../docs/BUG/BUG_03.md) — 배포 환경 LoRA on/off 한계 및 선택지

---

### 7-9. 학습 모니터링 및 조기 종료 (`train_monitor.py`)

#### 개요

`training/train_monitor.py` 는 학습 명령어를 **그대로 입력** 받아 학습 프로세스를 subprocess로 띄우고,
`trainer_state.json` 을 주기적으로 읽어 과적합을 감지하면 자동으로 학습을 멈추는 래퍼 스크립트다.

```
[사용자]
    ↓  python training/train_monitor.py -- python -m training.lora_train ...
[train_monitor.py]
    ├─ subprocess.Popen → lora_train.py 실행
    ├─ 15초마다 trainer_state.json 폴링
    │     eval_loss / train_loss 파싱
    │     과적합 조건 검사
    │       ① eval_loss N회 연속 상승
    │       ② train/eval gap > 임계값
    ├─ 조건 충족 시 SIGTERM 전송 → 프로세스 정상 종료 대기
    ├─ VRAM 정리 (torch.cuda.empty_cache + synchronize)
    └─ 최종 보고 출력 (①학습상황 ②종료조건 ③입력명령어)
```

> **왜 subprocess인가?**
> SIGTERM 전송 시 HuggingFace Trainer 는 현재 체크포인트를 그대로 유지하고 종료.
> 별도 프로세스이므로 CUDA context 가 완전히 분리 → VRAM 해제 신뢰성 높음.

#### 사용법

```bash
# 기본 (-- 뒤에 학습 명령어 전달)
python training/train_monitor.py -- \
    python -m training.lora_train \
    --output_dir output/LoRA_v8 \
    --epochs 5 \
    --eval_split 0.1 \
    --weight_decay 0.01

# --cmd 로 문자열 전달
python training/train_monitor.py \
    --cmd "python -m training.lora_train --output_dir output/LoRA_v8 --epochs 5"

# 종료 조건 직접 설정
python training/train_monitor.py \
    --n_rise 2 \
    --gap 0.8 \
    --poll 30 \
    -- python -m training.lora_train --output_dir output/LoRA_v8
```

#### 인자 목록

| 인자 | 기본값 | 설명 |
|---|---|---|
| `--n_rise` | `3` | eval_loss 연속 상승 허용 횟수 (초과 시 종료) |
| `--gap` | `1.2` | train/eval loss gap 임계값 (초과 시 즉시 종료) |
| `--poll` | `15` | trainer_state.json 폴링 간격 (초) |
| `--cmd` | `None` | 학습 명령어 문자열 (`--` 방식과 택일) |

#### 종료 조건 상세

```
조건 ①: eval_loss 연속 상승
  eval_losses[-4:] = [1.71, 1.74, 1.78, 1.82]  → 3회 연속 상승 → 종료
  best=1.71 → 현재=1.82 로 보고

조건 ②: train/eval loss gap 초과
  train=0.35, eval=1.72  → gap=1.37 > 1.2  → 즉시 종료
```

> `--eval_split 0` (eval 없음) 이면 `trainer_state.json` 에 eval_loss 가 없으므로 두 조건 모두 발동 안 됨.
> 모니터가 의미 있으려면 **반드시 `--eval_split 0.1` 이상** 설정.

#### 최종 출력 예시

```
==============================================================
[ 모니터 최종 보고 ]

① 학습 상황
   eval 체크포인트 수 : 12
   best eval_loss     : 1.6870  (step 800)
   최종 eval_loss     : 1.8220
   최종 train_loss    : 0.3620

② 종료 조건: eval_loss 3회 연속 상승 (best=1.6870 → 현재=1.8220)

③ 입력 명령어: python -m training.lora_train --output_dir output/LoRA_v8 --epochs 5 ...
==============================================================
```

#### 권장 조합

```bash
python training/train_monitor.py \
    --n_rise 3 \
    --gap 1.2 \
    -- \
    python -m training.lora_train \
    --output_dir output/LoRA_v8 \
    --max_samples -1 \
    --epochs 5 \
    --eval_split 0.1 \
    --weight_decay 0.01 \
    --lora_dropout 0.1
```

`--n_rise 3` + `--gap 1.2` 는 v7 실측치(best step 800, gap 1.33)를 기준으로 설정한 값.
데이터 규모나 에폭 수에 따라 조정 — epoch 수가 많을수록 `--n_rise` 를 낮게 잡는 것이 안전.

---

## 8. 대화 품질 목표 대비 현황 분석

> 일반 대화형 챗봇과 달리 이 시스템이 목표로 하는 품질의 핵심은 세 영역이다:
> **(1) 메모리** — 사용자를 기억하고 맥락을 이어가는 능력
> **(2) 실시간 감정표현** — 대화 흐름에 따라 감정이 자연스럽게 드러나는 능력
> **(3) 세계관·캐릭터 사용자 정의 입력 적응** — custom YAML 정의를 해석하고 일관되게 반영하는 능력
>
> 각 영역에 대해 현재 학습 데이터의 현황과 구조적 한계를 분석한다.

---

### 8-1. 메모리 영역

#### 현재 데이터

| 파일 | 건수 | 내용 |
|---|---|---|
| `common/memory_ref.jsonl` | 204건 | 이름·직업·사건 recall 패턴 (`"저번에 말했던 X 기억해?"`) |
| `memory/summarizer_sft.jsonl` | 15건 | 요약 태스크 — 대화 → 자연어 prose 변환 |

#### 한계 분석

**① VDB 컨텍스트 활용 패턴 없음**

런타임에서 `PromptBuilder._layer_c()`가 VDB 검색 결과를 system prompt에 주입한다:
```
[기억 회상 - VDB 검색]
사용자는 마케팅 취업 준비 중이다. 면접이 계속 실패해 지쳐 있다.
```
그러나 학습 데이터에 이 형식의 system prompt + 자연스러운 활용 응답 쌍이 존재하지 않는다.
모델은 이 섹션을 무시하거나 직접 인용하는 어색한 응답을 낼 수 있다.

**② session_context·character_notes 활용 패턴 없음**

`_layer_e()`로 주입되는 `[이전 대화 요약]`과 `[이번 세션 약속]` 섹션을 참조해
"아까 말했잖아" / "그때 약속한 거 기억해?" 식으로 자연스럽게 연결하는 예시가 없다.

**③ 요약 태스크 데이터 15건으로 부족**

LoRA 학습에서 한 카테고리가 유의미하게 반영되려면 최소 40~50건 이상을 권장한다.
현재 summarizer_sft는 기능 소실 방지 수준에 그친다.

**④ negative case 부재**

사용자가 언급하지 않은 정보를 "기억한다"고 잘못 주장하거나,
캐릭터의 특성을 사용자 정보로 혼동하는 패턴을 교정하는 데이터가 없다.

#### 필요한 추가 데이터

| 카테고리 | 형식 | 목표 건수 | 내용 |
|---|---|---|---|
| `memory/vdb_context_use` | system에 VDB 주입 형식 포함 | 40건 | VDB 결과를 자연스럽게 참조하는 응답 패턴 |
| `memory/session_context_ref` | system에 session_context 포함 | 30건 | 이전 요약을 이어가는 대화 패턴 |
| `memory/promise_followup` | character_notes 약속 이행 | 20건 | "~할게" 약속 후속 대화 |
| `memory/summarizer_sft` | 현재 15건 → 확장 | 50건 | 요약 품질 다양성 (부정 케이스 포함) |

---

### 8-2. 실시간 감정표현

#### 현재 데이터

| 파일 | 건수 | 내용 |
|---|---|---|
| `emotion/{중립·행복·감동·...}` 8종 | 각 40~46건 | 감정 상태별 단일 대화 |
| `emotion/transition.jsonl` | 40건 | 대화 중 감정 전환 |
| `affection/{stranger~intimate}` | 합계 ~550건 | 친밀도 단계별 반응 |

#### 한계 분석

**① 행동 묘사(\*...\*) 생성 패턴 부족**

실제 대화 로그에서 `*옅은 미소를 띄며*`, `*묵묵히 바라보며*` 같은 행동/감정 서술이 자주 등장한다.
현재 학습 데이터에서 이 패턴이 체계적으로 다뤄지지 않아 모델이 생성 빈도와 위치를 일관되게 학습하지 못한다.

**② 감정 억제 및 내면 표현 패턴 부족**

내향적·쿨한 캐릭터의 특징은 감정을 직접 말하지 않고 행동이나 짧은 언급으로 드러내는 것이다.
`emotion/` 데이터는 감정을 **표현하는** 쪽에 편중되어 있고,
감정이 드러날 듯 말 듯 억제되는 미묘한 패턴이 부족하다.

**③ 친밀도 × 감정 교차 데이터 없음**

동일한 사건(예: 사용자 칭찬)에 대해 친밀도가 낮을 때와 높을 때의 감정 표현 강도 차이를 보여주는
교차 데이터가 없다. affection 데이터와 emotion 데이터가 독립적으로 존재한다.

**④ 누적 감정 변화(감정 아크) 없음**

한 대화 내에서 여러 트리거에 의해 감정이 쌓이고 변하는 장대화 패턴이 없다.
`emotion/transition`은 단일 전환이고 `long_dialogue/`는 감정 흐름보다 화제 연결에 초점을 둔다.

#### 필요한 추가 데이터

| 카테고리 | 형식 | 목표 건수 | 내용 |
|---|---|---|---|
| `emotion/action_desc` | 행동 묘사 포함 응답 | 40건 | `*...*` 형식 감정 표현 패턴 |
| `emotion/suppressed` | 감정 억제·내면화 패턴 | 30건 | 감정을 말하지 않고 드러내는 패턴 |
| `emotion/affection_cross` | affection tier × emotion 조합 | 40건 | 친밀도 낮을 때 vs 높을 때 동일 상황 감정 차이 |
| `long_dialogue/emotion_arc` | 3~5회 감정 전환 포함 장대화 | 20건 | 한 대화 내 감정 누적·변화 흐름 |

---

### 8-3. 세계관·캐릭터 사용자 정의 입력 적응

#### 현재 데이터

| 파일 | 건수 | 내용 |
|---|---|---|
| `common/world_trigger_response.jsonl` | 50건 | 세계관 트리거(장소·배경) 반응 |
| `common/persona_follow.jsonl` | 202건 | 페르소나 일관성 (무례한 입력, AI 노출 방지) |
| `affection/formal/` 6종 | 합계 180건 | 존댓말 캐릭터 친밀도 단계별 반응 |

#### 한계 분석

**① 세계관 컨텍스트 주입 활용 패턴 없음**

런타임에서 `_layer_b()`가 world_context, act_context, location_context를 system에 주입한다:
```
[세계 배경]
해안 마을. 바다와 어선이 있는 작은 항구 도시.

[현재 위치]
village_cafe: 마을 중앙 카페. 조용하고 따뜻한 분위기.
```
모델이 이 정보를 1~2문장 이내로 대화에 자연스럽게 녹이는 학습 예시가 없다.
현재 world_trigger_response는 트리거 감지 후 반응이지 컨텍스트 활용이 아니다.

**② {char_name} 외 플레이스홀더 적응 미검증**

학습 데이터 system prompt는 `너는 {char_name}이다.` + 범용 제약 + 카테고리 행동 속성으로 구성된다.
실제 추론 시 speech_style, affection, emotion 슬롯이 캐릭터 YAML에서 채워지는데,
이 값이 학습 데이터의 카테고리 어휘와 얼마나 정렬되는지는 캐릭터마다 수동 검증이 필요하다.

**③ 사용자 정의 캐릭터(Haru 외) 대화 데이터 없음**

MookHyeon처럼 존댓말·무뚝뚝한 캐릭터는 `affection/formal/`로 추가됐지만,
YAML 정의만으로 추가되는 신규 캐릭터가 실제 대화에서 얼마나 안정적으로 동작하는지
검증하는 시스템이 없다. 신규 캐릭터 추가 후 소규모 검증 대화셋이 필요하다.

**④ 나레이션 스타일 다양성 부족**

world_trigger_response 50건은 장소/배경 유형이 단조롭다.
계절, 날씨, 사건, 문화 행사 등 다양한 세계관 요소에 반응하는 패턴이 필요하다.

#### 필요한 추가 데이터

| 카테고리 | 형식 | 목표 건수 | 내용 |
|---|---|---|---|
| `common/world_context_use` | system에 world/act context 포함 | 40건 | 장소·세계관 설명을 대화에 자연스럽게 반영 |
| `common/world_trigger_response` | 현재 50건 → 확장 | 80건 | 다양한 세계관 요소 유형(계절/문화/사건) 추가 |
| `character_validation/` | 신규 캐릭터별 10건 내외 | 건별 | 신규 YAML 추가 시 페르소나 일관성 최소 검증 |

---

## 9. 학습 파이프라인 상세

### 9-1. 1차 학습 — 사전 준비 데이터 기반

#### 목표

기본 캐릭터 말투·페르소나 일관성·감정 반응·세계관 반응의 기반을 확립한다.
신규 버전 학습 또는 첫 학습 시 사용. 로그 데이터가 없어도 독립 실행 가능.

#### 파이프라인

```
[데이터 준비]
training/data/**/*.jsonl  (현재 3,632건, 66파일)
    │
    ├─ _excluded/ 자동 제외
    ├─ memory/ 포함 (summarizer_sft.jsonl 등)
    └─ character_validation/ 포함 (신규 캐릭터 추가 시)
    │
    ▼ rewrite_system_prompts.py 실행
        → {char_name} 플레이스홀더 형식 전체 파일 동기화
        → FORMAL_KEYS 확인 (formal 카테고리 시스템 프롬프트 분기)
    │
    ▼ 선택: EWC 준비 (연속 학습 시)
        uv run python -m training.ewc \
          --adapter output/LoRA_vN/adapter \
          --out output/LoRA_vN
        → fisher.pt / ref_params.pt 생성
    │
    ▼ lora_train (train_monitor 경유)
        --data_dir training/data
        --epochs 3
        --eval_split 0.1
        --weight_decay 0.01
        --lora_dropout 0.1
        --ewc_fisher / --ewc_ref_params / --ewc_lambda 500  ← 연속학습 시만
        --category_weights (아래 권장값)
    │
    ▼ 자동 평가 (학습 완료 후, train_monitor 경유 시 모니터가 실행)
        ai_tell_checker.py   — AI-tell 표현 0건 목표
        memory_test.py       — 기억 정확도 4/5 이상
        scenario_eval.py     — 고정 18개 시나리오 PASS 기준치
    │
    ▼ 수동 검증
        verify_phases.py  (12턴 자동 대화)
        speech_style 일관성 수동 확인
```

#### 데이터 카테고리 및 권장 가중치

총 **3,632건** (64파일). 신규 추가된 3개 영역(메모리·감정·세계관)은 원시 데이터가 적지만 일반 챗봇과 차별되는 핵심 능력이므로 고가중치로 오버샘플링한다.

| 카테고리 | 건수 | 권장 가중치 | 유효 건수 | 비고 |
|---|---|---|---|---|
| **`memory/vdb_context_use`** | 40 | **4.0** | 160 | VDB 기억 자연스러운 활용 — 최우선 |
| **`memory/promise_followup`** | 20 | **4.0** | 80 | 약속 이행 — 가장 희소 |
| **`memory/session_context_ref`** | 30 | **3.5** | 105 | 이전 대화 요약 참조 |
| **`memory/summarizer_sft`** | 50 | **3.0** | 150 | 요약 생성 능력 보존 |
| **`long_dialogue/emotion_arc`** | 20 | **4.0** | 80 | 감정 호 변화 — 가장 희소 |
| **`emotion/action_desc`** | 39 | **3.0** | 117 | \*…\* 행동 묘사 감정 표현 |
| **`emotion/suppressed`** | 30 | **3.0** | 90 | 감정 억제·내면화 패턴 |
| **`common/world_context_use`** | 40 | **3.5** | 140 | 세계관 RAG 결과 자연 활용 |
| **`emotion/affection_cross`** | 40 | 2.0 | 80 | 친밀도×감정 교차 — tier 혼란 방지로 보수적 |
| `common/world_trigger_response` | 80 | 1.5 | 120 | 세계관 분위기 반응 |
| `common/memory_ref` | 204 | 1.5 | 306 | 단일 세션 내 기억 참조 |
| `long_dialogue/` (emotion_arc 제외) | 211 | 1.5 | 317 | 장대화 맥락 유지 |
| `affection/` | 882 | 1.0 | 882 | 친밀도 단계 핵심 — 충분히 커버됨 |
| `emotion/` (위 3종 제외) | 390 | 1.0 | 390 | 감정 기반 — 충분히 커버됨 |
| `personality/` | 497 | 1.0 | 497 | 성격 유형 — 충분히 커버됨 |
| `common/persona_follow`, `initiative` | 282 | 1.0 | 282 | 페르소나 일관성 |
| `speech_style/` | 445 | 0.8 | 356 | 말투 조합 — 소폭 다운샘플 |
| `common/ai_tell_removal` | 170 | 0.7 | 119 | AI투 억제 — 충분히 커버됨 |

**유효 총계 약 3,871건. 신규 3개 영역(메모리+감정+세계관) 비중 약 28%.**

> 가중치 조정 원칙: 고정값으로 1차 학습 후 eval(메모리 recall / 감정 표현 / 세계관 활용) 결과를 보고
> 다음 버전에서 부족한 영역의 가중치를 올리는 방식으로 버전 간 적응한다. 학습 중 동적 조정은 현재 규모에서 불필요.

권장 실행 명령어:
```bash
--category_weights '{
  "vdb_context_use": 4.0,
  "promise_followup": 4.0,
  "emotion_arc": 4.0,
  "session_context_ref": 3.5,
  "world_context_use": 3.5,
  "summarizer_sft": 3.0,
  "action_desc": 3.0,
  "suppressed": 3.0,
  "affection_cross": 2.0,
  "world_trigger": 1.5,
  "memory_ref": 1.5,
  "long_dialogue": 1.5,
  "affection/formal": 1.5,
  "speech_style": 0.8,
  "ai_tell_removal": 0.7
}'
```

#### 학습 방식

| 항목 | 설정 | 이유 |
|---|---|---|
| `--epochs` | 3 | v8 실측: 5 epoch 과적합 |
| `--lora_r` | 32 | v11 기준 — 3B 표현력 최대화 |
| `--lora_alpha` | 64 | r의 2배 |
| `--eval_split` | 0.1 | best checkpoint 자동 선택 필수 |
| EWC | 연속학습 시 λ=500 | 이전 버전 패턴 보호 |
| 풀리트레인 | 첫 학습 또는 대규모 데이터 갱신 시 | EWC 없이 처음부터 |

#### 평가 기준치

| 평가 항목 | 도구 | 합격 기준 | 재학습 기준 |
|---|---|---|---|
| AI-tell 표현 억제 | `ai_tell_checker.py` | 0/10 (0건) | 2건 이상 |
| 기억 정확도 | `memory_test.py` | 4/5 (80%) 이상 | 3/5 이하 |
| 고정 시나리오 | `scenario_eval.py` | 15/18 (83%) 이상 | 12/18 이하 |
| — 메모리 영역 | `scenario_eval.py --area memory` | 5/6 이상 | 3/6 이하 |
| — 감정 영역 | `scenario_eval.py --area emotion` | 5/6 이상 | 3/6 이하 |
| — 세계관 영역 | `scenario_eval.py --area world` | 5/6 이상 | 3/6 이하 |
| 추론 속도 | `speed_bench.py` | 8 tok/s 이상 | 5 tok/s 미만 |
| 실환경 12턴 대화 | `verify_phases.py` | 캐릭터 이탈 0회 | 이탈 2회 이상 |
| eval_loss best | 학습 로그 | v11 기준 1.54 이하 | 1.65 초과 |
| KLUE 회귀 (선택) | `klue_regression.py` | 기준점 대비 -5% 이내 | -5% 이상 하락 |

> KLUE 회귀 테스트는 자동 파이프라인에 포함되지 않음 (lm-eval 의존성 무거움).
> 베이스 모델 기준점은 첫 학습 전 1회만 측정: `uv run python training/eval/klue_regression.py --save_baseline`
> 이후 버전에서 선택적으로 실행: `uv run python training/eval/klue_regression.py --adapter output/LoRA_vN/adapter --baseline training/eval/klue_baseline.json`

---

### 9-2. 2차 학습 — 로그 데이터 기반

#### 목표

실사용 대화 분포에 모델을 적응시키고 수집된 피드백을 반영한다.
1차 학습 어댑터를 기반으로 EWC 연속학습 방식으로 진행한다.
로그 수집 기준치를 충족하면 v+1 어댑터를 발행한다.

#### 파이프라인

```
[로그 수집]
training/log/{daily,emotion,advice,memory,feedback_pos,feedback_neg}/*.jsonl
    │
    ├─ conversation_logger.py 자동 수집 (enable_play_log=True)
    └─ 수동 reviewed 마킹 (feedback_neg 필수)
    │
    ▼ 수집 기준치 충족 여부 확인 (아래 기준 참조)
    │
    ▼ build_sft_from_feedback.py 실행
        → feedback_pos → SFT 데이터 변환 (system 프롬프트 자동 삽입)
        → feedback_neg → 검토 대상 목록 생성 (DPO 준비용)
        → 출력: training/log/built/log_sft.jsonl
    │
    ▼ EWC 준비 (1차 학습 어댑터 기준)
        uv run python -m training.ewc \
          --adapter output/LoRA_vN/adapter \
          --out output/LoRA_vN
    │
    ▼ lora_train (train_monitor 경유)
        --data_dir training/data          ← 1차 데이터 포함 (희석 방지)
        --extra_data training/log/built   ← 로그 SFT 추가 (별도 인자 필요시 병합)
        --epochs 2                        ← 과적합 위험 낮추기
        --eval_split 0.1
        --ewc_lambda 1000                 ← 1차 학습보다 강한 보호
        --category_weights (아래 권장값)
    │
    ▼ 평가 (1차와 동일 + 추가)
        feedback_neg 케이스 재입력 → 개선 여부 확인
        daily/emotion 샘플 10건 수동 품질 확인
```

> `--extra_data` 인자가 없으면 log SFT 파일을 `training/data/log/` 하위에 복사해 포함시킨다.
> 로그 데이터와 1차 데이터를 함께 학습시켜 1차에서 확보한 기반이 희석되지 않도록 한다.

#### 데이터 카테고리 및 권장 가중치

| 로그 카테고리 | 수집 대상 | 권장 가중치 | 비고 |
|---|---|---|---|
| `feedback_pos` | 자연스러운 응답 확인된 턴 | 3.0 | 핵심 강화 신호 — 가중치 높게 |
| `daily` | 일상 잡담 로그 | 1.0 | 분포 적응 |
| `emotion` | 감정 반응 로그 | 2.0 | 감정 품질 직접 연결 |
| `memory` | 기억 참조 로그 | 2.5 | 메모리 활용 패턴 |
| `advice` | 고민 상담 로그 | 1.5 | 공감 및 의견 제시 |
| `feedback_neg` | **DPO 준비용만** — SFT 직접 포함 금지 | — | 잘못된 응답을 SFT에 넣으면 역효과 |

#### 로그 수집 기준치

2차 학습 실행 전 각 카테고리가 아래 기준을 충족해야 한다.

| 카테고리 | 최소 건수 | 필수 조건 |
|---|---|---|
| `feedback_pos` | **20건** | reviewed=true 또는 명시적 긍정 반응 확인 |
| `daily` | 30건 | 일상 대화, 기능 모드 응답 제외 |
| `emotion` | 20건 | 감정 트리거 유형 3종 이상 포함 |
| `memory` | 15건 | 이전 대화 참조가 실제로 일어난 턴만 |
| `feedback_neg` | 10건 | **note 필드 필수** — 문제 내용 기술 없으면 제외 |

**수집 제외 기준 (conversation_logger 필터 또는 수동 삭제):**
- 기능 모드 응답 (HTML, URL, JSON 포함)
- 10자 미만 단순 단답 단독 턴 (컨텍스트 없는 경우)
- 시스템 테스트·디버깅 대화 (사용자가 명시한 경우)
- 캐릭터 이탈 응답이 포함된 전체 세션 (feedback_neg로만 활용)

**feedback_neg 활용 원칙:**

```
feedback_neg 데이터는 SFT 학습에 직접 포함하지 않는다.
용도: ① 문제 패턴 파악 → 1차 데이터 보강 방향 결정
      ② DPO (chosen/rejected 쌍) 준비 — 동일 입력에 대해
         feedback_pos(chosen) + feedback_neg(rejected) 쌍을 구성
         조건: 동일 입력에 대한 pos/neg 쌍 20건 이상 누적 시 진행
```

#### 학습 방식

| 항목 | 설정 | 1차 대비 차이 |
|---|---|---|
| `--epochs` | 2 | 로그 데이터 소량 → 과적합 위험 ↑ |
| EWC λ | 1000 | 1차(500)보다 강하게 기반 보호 |
| 기반 데이터 | 1차 데이터 + 로그 SFT 병합 | 로그만 단독 학습 금지 |
| `--eval_split` | 0.1 | 동일 |

#### 평가 기준치

1차 학습 기준치에 추가:

| 추가 평가 항목 | 방법 | 합격 기준 |
|---|---|---|
| feedback_neg 재현 여부 | 수집된 neg 케이스 재입력 | 문제 응답 재발 0건 |
| feedback_pos 유사 케이스 | 유사 입력 수동 비교 | 응답 방향성 일치 70% 이상 |
| 기존 카테고리 퇴행 확인 | ai_tell_checker + memory_test + scenario_eval | 1차 기준치 유지 |

---

### 9-3. 학습 버전 발행 기준 요약

```
[1차 학습 발행 조건]
  ① training/data에 신규 카테고리 또는 50건 이상 데이터 추가
  ② ai_tell_checker 0/10 + memory_test 4/5 달성
  ③ verify_phases.py 캐릭터 이탈 0회

[2차 학습 발행 조건]
  ① 로그 수집 기준치 전 카테고리 충족
  ② feedback_pos 20건 이상 + reviewed 완료
  ③ 1차 평가 기준치 유지 확인 (퇴행 없음)

[DPO 발행 조건 (미래)]
  ① feedback_pos/neg 동일 입력 쌍 20건 이상
  ② SFT(1차 또는 2차) 어댑터 완성 상태에서 추가 학습
```
