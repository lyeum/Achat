# Achat 로드맵

> 작성일: 2026-04-21 (코드 실측 기반 최신화)
> 정렬 기준: 구현 난이도 및 변경 영향력이 낮은 것부터 높은 것 순서
> 완료 항목은 ✅, 미구현은 빈 칸

---

## 범례

| 기호 | 의미 |
|---|---|
| ✅ | 완료 |
| 🔧 | 코드 수정만 필요 (재학습 불필요) |
| 📊 | 데이터 작업 필요 |
| 🏗️ | 구조 변경 필요 |
| 🎓 | 모델 연장학습 필요 |
| 🚀 | 배포/인프라 작업 |
| ⏸️ | 조건부 진행 (선행 조건 있음) |

---

## 1단계 — 즉시 코드 수정 ✅

### 1-1. 대화 할루시네이션 감소 🔧

| 항목 | 파일 | 내용 | 비고 |
|---|---|---|---|
| K ✅ | `conversation/core/prompt_build.py` | `_layer_a()` rules: `" ".join` → `"\n- "` 목록 형식 | 존댓말·영어 혼입 동시 개선 |
| L ✅ | `conversation/core/llm_client.py` | `_generate_llama()` `repeat_penalty=1.15`, `repeat_last_n=64` 추가 | llama.cpp 반복 루프 해소 |
| L ✅ | `conversation/core/llm_client.py` | `_generate_transformers()` `repetition_penalty` 1.1→1.15 | transformers 반복 루프 해소 |
| M ✅ | `conversation/core/prompt_build.py` | `BUDGET["layer_d"]` 300→450 | 맥락 끊김 완화 |
| N ✅ | `conversation/core/prompt_build.py` | `_layer_b()` RAG 재가공 허용 지시 문구 수정 | 세계관 재가공 표현 자연스러움 |

### 1-2. 메모리 품질 정상화 🔧

| 항목 | 파일 | 내용 |
|---|---|---|
| S ✅ | `conversation/core/router.py` | `_run_summarizer()` → 백그라운드 스레드 이동 (매 5턴 blocking latency 제거) |
| T ✅ | `memory/summarizer.py` | score 기본값 0.5→0.0, write threshold 0.5→0.65 (VDB 오염 차단) |

---

## 2단계 — 학습 데이터 보강 (재학습 불필요, 다음 학습 전 준비) ✅

| 항목 | 내용 |
|---|---|
| P ✅ | `correction_graceful.jsonl` — 세계관 정보 모를 때 "모른다"고 말하는 패턴 7개 추가 |

---

## 3단계 — 메모리 구조 개선 (새 필드 추가, 재학습 불필요) ✅

단기→중기→장기 3계층 메모리 완성. 설계 세부사항: `docs/plan/대화개선.md` 7~8절.

| 항목 | 파일 | 내용 |
|---|---|---|
| Q ✅ | `memory/short_term.py`, `session.py` | `session_context: str` 필드 + `evict_to_context()` (5턴 초과 시 raw text 누적) |
| R ✅ | `conversation/core/prompt_build.py` | `_layer_e()` 추가 — `session_context` + `character_notes` 통합 주입 |
| 6-6 ✅ | `session.py`, `router.py` | `character_notes: list[str]` 필드 + 약속 패턴 감지 (`~할게`, `~기억할게` 등) |
| U ✅ | `memory/summarizer.py` | 요약 프롬프트 필드 분리 (`고유정보/사건/캐릭터_약속/감정/선호도`) + `_to_prose()` 추가 — **기존 개선8 구조(`이름/사건/감정/기타`)를 대체** |
| V ✅ | `memory/long_term.py` | 세션 시작 시 TTL 만료 항목 삭제 (score<0.65 + 30일 초과) |
| W ✅ | `memory/long_term.py` | VDB 저장 전 cosine 0.85 이상이면 기존 항목 update |
| X ✅ | `memory/long_term.py` | 캐릭터별 VDB quota 200개 상한, 초과 시 score 낮은 항목 자동 삭제 |
| Y ✅ | `conversation/core/router.py` | aff_delta≥5 또는 trigger_event 시 VDB 즉시 flush |
| Z ✅ | `session_manager.py`, `bridge.py` | 세션 종료 시 `session_context` → `session_state.json` 스냅샷 저장 |

---

## 4단계 — 세션 관리 개선 (DB_renew) ✅

> 설계 세부사항: `docs/plan/DB_renew.md`
>
> **목적**: 현재 `session_manager.py`에 내부 API(`get_active`, `new_session` 등)는 있지만 UI에서 접근·전환이 불가능하다.
> 이 단계의 목표는 사용자가 "학교 시나리오", "집 시나리오" 같은 복수 세션을 앱 UI에서 직접 선택하고 전환·초기화할 수 있도록 하는 것이다.
>
> Phase 2 (`clear_session()`)·Phase 6 (`enable_play_log` 플래그)는 이미 구현됨.

| 항목 | 내용 |
|---|---|
| DB-1 ✅ | `session_manager.py` — `reset_current()` (대화 초기화), `delete_session()` (세션 삭제) 추가 |
| DB-3 ✅ | Agent 세션 주입 방식 — `SessionManager` 경유, `bridge._restore_session_from_state()` 구현됨 |
| DB-4 ✅ | `bridge.py` — `listSessions`, `switchSession`, `newSession`, `resetSession` QML 슬롯 연결 |
| DB-5 ✅ | SettingsPanel — "현재 대화 초기화" 버튼 + 세션 목록(전환/삭제) UI 추가 |

---

## 5단계 — 배포 파이프라인 완성

> 참조: `docs/plan/phases.md` Phase 5~6

| 항목 | 내용 | 상태 |
|---|---|---|
| DEP-0 ✅ | 배포 환경 동작 검증 체크리스트 작성 — `docs/plan/deploy_checklist.md` | 완료 |
| DEP-1 | LoRA v11 병합 실행 — `uv run python scripts/merge_lora.py --adapter output/LoRA_v11/adapter --output_dir output/merged_v11` | 실행 대기 |
| DEP-2 | GGUF 변환 — `bash scripts/convert_to_gguf.sh --merged output/merged_v11 --out_dir output/gguf --llama_cpp <경로>` | DEP-1 이후 |
| DEP-3 ✅ | `pyproject-deploy.toml` 수정 — `whoosh` 제거, SQLite FTS5 주석 명시 | 완료 |
| DEP-4 | 배포 패키지 end-to-end 검증 — `deploy_checklist.md` 3~4단계 수행 | DEP-2 이후 |
| DEP-5 | LoRA 런타임 on/off: `llama-cpp-python` PR 머지 후 적용 ⏸️ | PR 대기 중 |
| DEP-6 ✅ | CD 스크립트 — `scripts/package_deploy.sh` (rsync 기반 dist 생성 + zip) | 완료 |

---

## 6단계 — 모델 연장학습 (강화학습)

> **형태**: SFT 연장학습 또는 강화학습(DPO/GRPO 등) — 방식은 데이터 누적 후 결정
> **조건**: feedback_pos / feedback_neg 각 20건 이상 누적 후 진행 (배포 불필요 — 개발 환경 로그로 충족 가능, 5단계와 병렬 진행 가능)
> **참조**: `docs/학습후보.md`, `docs/plan/training_개선.md`
>
> 현재 EWC 다단계 학습 (7-2), 카테고리 가중치 샘플링 (7-3), 학습 모니터링 (7-9)은 이미 구현 완료.
> KL Divergence (7-4), VRAM 절감 (7-5), WSD 스케줄러 (7-6) 실험은 DPO 이후 검토.

| 항목 | 내용 |
|---|---|
| H | `build_dpo_dataset.py` 작성 — feedback_pos/neg 로그에서 chosen/rejected 쌍 구성 |
| I | 강화학습 파이프라인 결정 — DPO, GRPO, ORPO 중 VRAM 6~8GB 이내에서 실행 가능한 방식 선택 |
| J | 연장학습 실행 — 기존 데이터 + feedback_sft + stranger tier + H 결과 통합 |
| - | KL Divergence 규제 / WSD 스케줄러 / VRAM 절감 기법 실험 ⏸️ (DPO 이후 검토) |
| - | 한국어 특화 베이스 모델 재검토 (현 Qwen2.5-3B 대비, EXAONE-3.5-2.4B 후보 — `docs/학습후보.md` 참조) |

---

## 7단계 — 대화 품질 점검

> **조건 완화**: QA-1, QA-2는 배포 전 현재 `training/log/` 데이터로 진행 가능.
> QA-3~5는 6단계 완료 이후 또는 사용자 로그 누적 시작 후 진행.

| 항목 | 내용 | 조건 |
|---|---|---|
| QA-1 | `feedback_neg` / `feedback_pos` 로그 전수 검토 | 즉시 가능 |
| QA-2 | 대화 품질 벤치마크 데이터셋 선정 — 현재 `training/log/` 데이터 중 품질 높은 것을 기준으로 고정 | 즉시 가능 |
| QA-3 | 벤치마크 기준으로 연장학습 결과 정량 평가 | 6단계 이후 |
| QA-4 | 사용자 로그 데이터 품질 검증 파이프라인 구축 (`review.py` 확장 또는 별도 스크립트) | 6단계 이후 |
| QA-5 | 강화학습 진행 (6단계 H~I, feedback 기반 DPO/RLHF 반복) | 6단계 이후 |

---

## 반려 / 영구 보류 항목

| 항목 | 사유 |
|---|---|
| 웹 검색 기능 | RAM/VRAM 절감 목적, 2026-04-06 삭제 완료 |
| llama-server HTTP 모드 | 배포 복잡도 증가, 오버엔지니어링 |
| ctypes 직접 호출 | 유지보수 부담 과도 |
| Qwen2.5-3B-Korean 교체 | 학습량 비대칭, 낮은 성능 평가 |
| summarizer 2-LLM-call 방식 | 오버스펙 — session_context는 LLM 불필요 |
| 절전 모드 | RAM 실제 문제 발생 시점에 재검토 |
| 경량 임베딩 모델 교체 | 현 BAAI/bge-m3 대비 리스크 있음 |
| O — `*...*` 포맷 통일 | 매 발화 생성 아닌 선택적 표현이어야 함. 학습 데이터 추가 시 일관성 오염 우려. 차후 강화학습 단계에서 재검토 |
| NAR-1 — `**...**` 응답 fine-tune | 환경적 한계로 LLM 기반 나레이션 확장 중단. 현재 NarrationMonitor 키워드 방식만 유지 |
| NAR-2 — Narrator LLM 통합 | 동상 (키워드 방식으로 대체 운용 중) |
| NAR-3 — 나레이션 자발 생성 | 동상 |
| 사용자 로그 수집 서버 (LOG-1~5) | 보안 설계 미해결 (서버 공격 대응 방안 없음). 해결 방안 확보 전 보류 |

---

## 단계별 선행 조건

```
1단계 ✅ ──┬──► 2단계 ✅ (데이터 보강)
           └──► 3단계 ✅ (메모리 구조)
                      └──► 4단계 ✅ (세션 관리)

5단계 (배포) ─── 1~4단계 ✅ 완료 후
  └── DEP-0: 배포 환경 동작 검증 (선행 필수)
  └── DEP-6: CD 파이프라인 (DEP-4 이후)

6단계 (연장학습) ─── feedback_pos/neg 각 20건 이상 누적 후
                     5단계와 병렬 진행 가능

7단계 (품질 점검)
  QA-1, QA-2 ─── 즉시 가능 (현재 training/log/ 활용)
  QA-3~5 ─────── 6단계 이후
```
