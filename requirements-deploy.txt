# ============================================================
# requirements-deploy.txt — 배포 환경 (Windows + CPU)
# 용도: GGUF 모델 로컬 추론, PySide6 위젯 실행
#
# llama-cpp-python CPU 빌드 설치:
#   pip install llama-cpp-python
# AVX2 이상 CPU 필수, AVX-512 권장
# ============================================================

# ── LLM 추론 (CPU) ──────────────────────────────────────────
llama-cpp-python>=0.3.0

# ── UI ──────────────────────────────────────────────────────
PySide6>=6.7.0

# ── 임베딩 / VDB ────────────────────────────────────────────
sentence-transformers>=3.0.0  # bge-m3 로컬 임베딩
chromadb>=0.5.0               # 장기 메모리 + RAG VDB

# ── 데이터 처리 ─────────────────────────────────────────────
PyYAML>=6.0
numpy

# ── 유틸리티 ────────────────────────────────────────────────
loguru>=0.7.0
